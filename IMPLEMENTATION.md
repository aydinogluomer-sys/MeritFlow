# MeritFlow — Implementation Status & TODO

> **Yaşayan durum/todo takip dosyası.** Detaylı "neden/nasıl" için kaynak: `docs/planning/` (00–18),
> `docs/adr/` (ADR-001…020), `CLAUDE.md`. Çelişki olursa `docs/planning/00_DECISION_LOCK.md` kazanır.
> Bu dosya **kod değildir**; yalnızca nerede olduğumuzu ve ne yapacağımızı izler.
> Son güncelleme: 2026-09-15 (Phase P4 follow-up — graceful comparison degradation in the semantic query SERVICE; §23/§26; APP/domain-layer ONLY, NO migration/DB object/metric/permission/feature flag/dependency → no type-drift, catalog 23/14 metrics. SUPERSEDES the hard-fail flagged by the #57 FIX + 8-C1: a comparison that is NOT executable for an AMBIGUOUS window (comparison_not_executable — a range/relative selector has no well-defined "previous") is now NON-FATAL. The primary metrics resolve WITHOUT a delta and the outcome carries an EXPLICIT non-silent signal — ADDITIVE ok:true fields comparisonUnavailable:true + warnings[] carrying the comparison_not_executable ExecutionError — so a caller never silently loses a delta (§23). GENUINE errors STILL fail (period_not_found / any non-ambiguous code → whole-query ok:false, never swallowed); the earliest-period null case stays a BENIGN no-delta (deliberately NOT flagged). The #57 anchoredMetricPeriod anchoring is UNTOUCHED (it still produces real deltas — a safety net, not a replacement); consumers already tolerate an absent delta (readOrgMetric) → NO page change. The outcome-type change is ADDITIVE (fields set ONLY when degraded → an untouched ok:true keeps its exact prior shape). Regression FLIPPED (relative+comparison: ok:false comparison_not_executable → ok:true + signal, delta dropped) + a degradation matrix added, all against the REAL service via fakeSupabase: anchored+comparison → ok:true WITH delta (no signal); earliest-period → ok:true no delta (not flagged); nonexistent anchor+comparison → still ok:false period_not_found. verify:fast (unit 749) + verify:contracts + npm audit high 0 + next build green. NO LLM/raw SQL (§26); SI-12 intact. ITEM 2 (INVESTIGATION only — NO code, NO fabrication §23): adjustment-money-impact = NO honest SI-12 source today (manual adjustments are POINTS in point_ledger; bonus_ledger has NO money-level adjustment event — only accrual/reversal writable; money only materializes through a full pro-rata re-run, not isolatable) → UNBLOCK-SPEC (a money-level adjustment ledger event). dispute-financial-impact = 7-C/7-D/7-E landed a bonus_ledger money footprint (dispute_recalc-tagged reversal + superseding accrual) → PROPOSE a SEPARATE 0050-style definer-rights slice for a period-level NET dispute-recalc money delta (v_finance_dispute_recalc_money + a new metric + golden pgTAP) — honest + SI-12-safe but period-level, NOT per-dispute (no dispute_id on money rows) and NOT gross-reversed (which overstates); per-dispute attribution stays an UNBLOCK-SPEC. Full ITEM 2 findings in the slice closeout).
> Prev: Phase P4 8-C3 deterministic insight engine (§10.12) — the LAST slice of Module 8 (8-C1 ops ✅ → 8-C2 policy ✅ → 8-C3 insight engine ✅ = MODULE 8 COMPLETE); §10.12/§2.3/§2.4/§2.8/§12.8/§23/§24/§26; APP/domain-layer ONLY, NO migration/DB object/metric/permission/feature flag/dependency → no type-drift, catalog 23. A VERSIONED deterministic rule catalog (domain/insight-rules.ts, PURE, INSIGHT_RULE_SET_VERSION='insight-v1') — 6 threshold/delta rules with explicit thresholds + rationale: budget_risk (budget_variance ≥10 warn/≥20 crit), cycle_slowdown (cycle_completion_rate Δ≤−15/≤−30), dispute_concentration (dispute_rate ≥20/≥40), concentration_change (payout_concentration HHI ≥0.5/≥0.7), significant_delta (manual_override_rate Δ≥+15/≥+30), threshold_breach (gaming_flag_rate ≥15/≥30). NO statistical anomaly/forecast (P8); NO LLM (§26). Engine (application/insight-engine.ts, server-only): resolves the current bonus_period, reads all 6 metrics via executeSemanticQuery (anchored bonus_period + previous_period comparison — NO raw SQL §26; injected service_role admin client for ORG-WIDE detection, executors still .eq(organization_id) → tenant-safe; metric read INJECTABLE for tests), runs the pure catalog → candidates, emits each IDEMPOTENTLY. IDEMPOTENCY (dedup option i, app-layer, no migration): new repo method recordRecurring finds a NON-TERMINAL insight of identity {org, insight_type, subject_type, subject_id, bonus_period_id} (null-safe .is()) → TOUCHES it (last_detected_at + refresh severity/payload/evidence, keep status) else INSERTS 'calculated' — a re-run creates NO duplicate (the property a naive insert() violates; a dismissed/retrospective terminal → a fresh insight). Every insight satisfies InsightSchema (≥1 evidence [the metric] + ≥1 action route §12.8; TR headline; deterministic facts stamped with the rule-set version); model_payload stays null. Emitted org-level insights (subject_type='organization') are RLS-gated on READ (intelligence.read); aggregate signals only (no per-employee; only percent/score facts — no raw money, SI-12). On-demand trigger runInsightEngineAction (intelligence.manage, server-side; injects createAdminClient). Barrel exports runInsightEngine + INSIGHT_RULE_SET_VERSION + runRules + types. Adversarial review (3 lenses + verify, 5 agents): 2 raised, 0 confirmed. verify:fast (unit 747) + verify:contracts + npm audit high 0 + next build green. CLOSES Module 8).
> Prev: Phase P4 8-C2 Policy Intelligence dashboard (§10.9) — 2nd of the 3 slices finishing Module 8 (8-C1 ops ✅ → 8-C2 policy → 8-C3 insight engine); §10.1/§10.3/§10.9/§10.16/§18/§19/§23/§26; APP-LAYER ONLY, REUSES the existing policy engines' public APIs (no re-implementation — cross-links to the detail pages); NO migration/DB object/metric/permission/feature flag/dependency → no type-drift, catalog 23. app/(app)/policy-intelligence (RSC): version-vs-version comparison across the §10.9 dimensions via getHealthComparison (Sağlık Motoru DimensionDelta — dispute_exposure / gaming_resistance / manager_discretion(override) / complexity / payout_concentration + financial_integrity; 0–100, higher=healthier → positive Δ = improvement; accessible table + DeltaBadge, glyph not color-only) + a complexity-across-versions trend via getPolicyComplexityTrend (TrendChart). Anchor = the freshest health-v1 evaluated policy; current(anchor) vs previous (pickPreviousVersion). hasPrevious=false → honest "need ≥2 değerlendirilmiş sürüm"; no evaluated version → "not yet evaluated" + cross-link. The semantic metric layer is NOT used (it can't slice dispute/gaming/concentration/override by policy_version) → single-sourced from the health engine (no inconsistent double-source). Structural diff DEFERRED to the cross-linked /policy-impact (optional deliverable; avoids a 3rd flag dependency + duplication §23). Cross-links to /policy-health, /policy-debt, /policy-impact (aggregate + link, NOT re-implement). Authz: page gates intelligence.read + 'intelligence' flag (nav consistency); every engine call wrapped in safe() → RLS (policy.manage) + sub-flag (health_engine/policy_debt) denial → honest UnavailableCard/EmptyState, never a fabricated number; client-side authz never SoT (AD1). Aggregate policy signals only (no per-employee). Nav "Politika Zekası" (GitCompare; intelligence.read + intelligence flag). Adversarial review (3 lenses + verify, 3 agents): 0 raised, 0 confirmed. verify:fast (unit 727) + verify:contracts + npm audit high 0 + next build green. NO LLM/Policy Doctor (P9); NO insight engine (8-C3); NO forecast (P8); NO raw SQL/LLM (§26). Insight engine = 8-C3, concludes Module 8).
> Prev: Phase P4 FIX — anchor executive + financial metric bundles to the current bonus_period (comparison_not_executable) — repairs a SHIPPED defect in the 8-B1 executive + 8-B2 financial dashboards; APP-LAYER ONLY, catalog 23, NO migration/metric/permission/dependency, NO change to the semantic-query service contract. Root cause: both pages built their PRIMARY metric bundle with period {kind:'relative', trailing:'current'} + comparison {basis:'previous_period'}; resolveComparisonPeriod (period-resolver.ts:104) rejects a non-anchored selector with comparison_not_executable and the service (semantic-query-service.ts:~180) turns that into a WHOLE-query ok:false → every primary card rendered UnavailableCard and "Ne değişti?" was always empty against a real DB (the mocked-OUTCOME unit tests missed it). Fix: ONE pure shared helper anchoredMetricPeriod(currentPeriodId) in executive/model.ts (re-exported via financial + operations models) → present → {period:{kind:'bonus_period', bonusPeriodId}, comparison:{basis:'previous_period'}}; absent → {period:{kind:'relative', trailing:'current'}} WITHOUT comparison. Wired into executive (new currentPeriodId resolve) + financial (existing periodId) primary bundles; operations refactored onto the same helper (behavior-preserving — no 3 divergent copies). REGRESSION test (the gap that hid the bug — exercises the REAL service via fakeSupabase, NOT a mocked outcome): relative+comparison → ok:false comparison_not_executable; anchored bonus_period+comparison → ok:true with a previous-period delta; + a pure anchoredMetricPeriod unit test + executive/financial/operations page-guards (use the helper, never inline relative+comparison). SI-12 unchanged (finance via v_finance_*, honest empty on RLS-deny, never a fake 0). verify:fast (unit 718) + verify:contracts + npm audit high 0 + next build green. NOTE: the more robust service-level graceful degradation (omit deltas instead of failing the whole query) is FLAGGED as a follow-up — it changes the core-service contract + its rejection tests, deliberately not bundled here).
> Prev: Phase P4 8-C1 Operations Intelligence dashboard (§10.8) — first of the 3 slices finishing Module 8 (8-C1 ops → 8-C2 policy → 8-C3 insight engine); §10.1/§10.3/§10.8/§10.10/§10.16/§18/§19/§23/§26; APP-LAYER ONLY over the 8-A metric layer + insight store, reusing the 8-B viz platform; NO migration/DB object/metric/permission/dependency → no type-drift, catalog 23. app/(app)/operations (RSC, role-adaptive) — 6 cards each REAL or honest UnavailableCard (SI-12, never a fake 0): Döngü Sağlığı (cycle_completion_rate), Onay Gecikmesi (approval_latency, ms→sn), Manuel Müdahale (manual_override_rate), Oyunlaştırma Bayrağı (gaming_flag_rate), İtiraz Oranı (dispute_rate) — all via executeSemanticQuery (none role-gated; one bundle) — + İstisna Hacmi (open warning/critical insight COUNT via IntelligenceRepository.list, RLS-scoped; unavailable — not 0 — on a failed read). Charts (§10.16 + §19 accessible): approval-latency trend (per-period reads — approval_latency isn't servable by bonus_period — omitting empty periods, never a fabricated 0) + permission-aware drill (org→team, NEVER employee — aggregate-only, no surveillance); open-exception breakdown distribution by insight type. "Ne değişti?" (comparison deltas) + "İnceleme gerektirenler" (attentionInsights). DEFERRED honestly (§23): İnceleme Gecikmesi / Hatırlatma Sayısı / Geciken / Yeniden Hesaplama / Yönetim Eforu Tasarrufu (§10.8 — NO false hours-saved claim). Nav "Operasyon Zekası" (intelligence.read + intelligence flag). Adversarial review (3 lenses + verify, 7 agents): 4 confirmed & FIXED — BLOCKER: previous-period comparison is only executable on a bonus_period selector (the service fails the WHOLE query on a relative selector) → anchored the bundle to the current bonus_period, comparison only when anchored; MAJOR: approval_latency delta was raw ms mislabeled 'sn' in "Ne değişti?" → ÷1000 to match the card; minor: İstisna Hacmi showed a fabricated 0 on a failed insight read → UnavailableCard. verify:fast (unit 712) + verify:contracts + npm audit high 0 + next build green. NOTE: the executive + financial pages share the SAME relative+comparison latent defect (out of 8-C1 scope — flagged for a follow-up fix slice). Policy dashboard = 8-C2, insight engine = 8-C3).
## 0. Yönetişim kuralı (her şeyden önce)

- **Kod yalnız kullanıcı tam cümleyle yetki verince yazılır** (ADR-020): `implementation authorized only for Phase X — <slice>`.
- **İzin faz/dilim-sınırlıdır** — bir dilimin onayı sonrakini kapsamaz.
- Her tenant tablosu **aynı dilimde** RLS ENABLE+FORCE + policy + bloklayıcı pgTAP ile gelir ("tablo şimdi, RLS sonra" yasak — doc 18 §11).
- DB komutları yalnız **local dev/staging**; production'a asla (ADR-014).

Durum işaretleri: ✅ done/verified · 🟡 committed (verify bekliyor) · ⛔ gated (henüz yetki yok) · ⬜ başlanmadı.

---

## 1. Nerede olduğumuz (özet)

**Planlama:** ✅ Tamamlandı. Phase 0–2 kapalı; 19 planning dokümanı (00–18), 20 ADR, `CLAUDE.md`.
Decision Lock = D1–D12 + AD1–AD10 (22 karar).

**Implementation (DB foundation, dilim-dilim yetkiyle):**

| Dilim | İçerik | Dosyalar | Durum | Kanıt |
| --- | --- | --- | --- | --- |
| Phase 3A | 11 foundation/RBAC tablosu + RLS helper + RLS + audit + seed | `migrations/0001–0007`, `seed`, `tests/0001` | ✅ verified | 38/38 pgTAP (2026-06-24) |
| Phase 3B-A | `scoring_policies` + `scoring_policy_versions` (published immutable AD7) + RLS + `policy.manage` | `migrations/0008`, `tests/0002` | ✅ verified | commit `dd9b861`; 2026-07-24 |
| Phase 3B-B | `point_ledger` (append-only, server-only, Finance excluded) + `team_of()` + RLS | `migrations/0009`, `tests/0003` | ✅ verified | commit `f46ab49`; 2026-07-24 |
| Phase 3 comp | `compensation_records` (raw SELECT closed, comp.read writes, DELETE blocked, masked write/access audit, justified `read_compensation_record`) | `migrations/0010`, `tests/0004` | ✅ verified | commit `c9cd0f2`; 2026-07-24 |
| Phase 3 bonus P/P | `bonus_periods` + `bonus_pools` (state machine, AD10 pool-lock-before-period-lock, locked t_org+metadata required, locked immutability, period identity immutability, period.manage/pool.create) | `migrations/0011`, `tests/0005` | ✅ verified | commit `d04b954`; 2026-07-24 |
| Phase 3 bonus C/E | `bonus_pool_components` + `bonus_pool_eligibility` (MVP Safe Pro-Rata individual=1.0; same-org employee via memberships composite FK; AD9 primary_team is_primary; server-only eligibility writes; inputs immutable once parent pool leaves draft) | `migrations/0012`, `tests/0006` | ✅ verified | commit `8f74e8d`; 2026-07-24 |
| Phase 3 bonus C/R/A/S | `bonus_calculation_runs` + `bonus_allocations` + `bonus_allocation_snapshots` (run state machine running/completed/superseded; AD10 locked-period+locked-pool double guard; idempotency `unique(org, idempotency_key)`; completed-run allocation freeze; thin snapshot append-only; cap-not-exceeded + pending_missing_cap_basis; approved/exported/paid blocked; server-only writes) | `migrations/0013`, `tests/0007` | ✅ verified | commit `e3bd1a3`; 2026-07-25 |
| Phase 3 bonus ledger | `bonus_ledger` (append-only double-entry money; deferred `Σdebit=Σcredit` per (org, transaction_id) balance trigger; accrual ⇒ snapshot_id; idempotent accrual; only bonus_accrual+reversal writable; Finance/Auditor raw read only, server-only writes) | `migrations/0014`, `tests/0008` | ✅ verified | commit `71e68f7`; 2026-07-26 |
| Phase 3 disputes | `disputes` + `dispute_events` (mutable state machine open→under_review→needs_info→resolved→closed; append-only auto-history trigger; D9 stored decision_owner_id + owns_review_decision; HR-only assign via has_role('hr'); due_at stored + sanity; server-only auto events; Finance/Support excluded) | `migrations/0015`, `tests/0009` | ✅ verified | commit `1bf63fe`; 2026-07-26 |
| Phase 3 anti-gaming | `anti_gaming_flags` (mutable review lifecycle open→reviewing→confirmed/dismissed; D5 no-auto-punish — isolated from all ledgers, no FK/write to point_ledger/bonus_ledger/bonus_*/compensation; review consistency + reviewer≠subject; server-only INSERT; review via has_role('hr') OR manages_team(team_of(subject)); Finance/Support excluded) | `migrations/0016`, `tests/0010` | ✅ verified | commit `0c813e9`; 2026-07-31 |
| Phase 3 notifications | `notifications` (recipient-only delivery sink; unread→read one-way lifecycle, recipient marks own read + read_at server-stamped; INSERT server-only; no client DELETE and no prevent_delete — retention/TTL V1; no audit trigger; no new permission/role; no type enum — type non-empty only; payload JSON object; same-org composite FK `(organization_id, recipient_id)→memberships`; RLS recipient-only SELECT/UPDATE — HR/Auditor/Manager/Finance/Support excluded) | `migrations/0017`, `tests/0011` | ✅ verified | commit `fe1b81e`; 2026-07-31 |
| Phase 3 exports | `exports` (payout export record/container — generation engine YOK; Finance INSERT via existing `payout.export`, actor integrity `exported_by = auth.uid()`; snapshot_id NOT NULL — SI-3; AD6/SI-15 gate via SECURITY DEFINER trigger checks `snapshot.calculation_run_id → bonus_allocations` for pending_missing_cap_basis by status OR cap_applied; `exports.bonus_period_id` = snapshot period; append-only client posture — no authenticated UPDATE/DELETE; prevent_delete retention; audit on INSERT; RLS Finance + Auditor SELECT, HR/Manager/Employee/Support excluded; no new permission — catalog stays 20) | `migrations/0018`, `tests/0012` | ✅ verified | commit `b66350d`; 2026-07-31 |
| Phase 4 task/review core | `tasks` + `task_events` + `task_reviews` (status machine draft→…→approved/rejected, DELETE forbidden; task_events auto-written append-only history — AD4; task_reviews append-only decisions; **review-driven transition** — task_reviews INSERT applies the task status via SECURITY DEFINER trigger; direct client approve/reject/needs_revision blocked; **self-approval hard block** — AD4; D3 approve⇒quality≠poor; complexity/impact/quality/timeliness enums from doc 04; same-org composite FKs; RLS assignee/creator/reviewer/team-manager/HR/Auditor/support-grant, **Finance excluded**; no new permission — catalog 20) | `migrations/0019`, `tests/0013` | ✅ verified | commit `148667e`; 2026-08-01 |
| Phase 5 scoring engine | `point_ledger` +`task_id`/`task_approved` + SI-1 idempotency; `tasks.final_points`→numeric (approve→`point_ledger task_approved` earning row + `final_points` cache + breakdown in `metadata`; SECURITY DEFINER BEFORE UPDATE trigger on the approved transition; doc-04 formula from published `scoring_policy_versions` multipliers/`revision_penalty_rule`; raw numeric no rounding; timeliness from the approving review — AD4; collaboration non-scoring — AD5; D3/AD7 guards; trusted direct-approve without review skips; Finance excluded; no new permission — catalog 20; no bonus changes) | `migrations/0020`, `tests/0014` | ✅ **verified** | commit `aa47e40`; 2026-08-01 |
| Phase 6 bonus engine | `run_bonus_calculation()` SECURITY DEFINER server-only: locked period+pool (AD10); approved points from `point_ledger task_approved` × `tasks.approved_at` in period; Safe Pro-Rata W_individual=1.0 (D1/D2); cap from `compensation_records`×cap_rate×proration (AD6 `pending_missing_cap_basis`, no unlimited cap); T_org+AD8 top-up; largest-remainder kuruş (tie-break employee_id); writes `bonus_allocations` + immutable snapshot; run completed + period locked→calculated; idempotent `(org,key)`; Σfinal+undistributed=pool_ref (SI-13); accrual → **Phase 6-b** (`a65013d`); Finance raw-excluded (SI-12); catalog 20 | `migrations/0021`, `tests/0015` | ✅ **verified** | commit `0c54fba`; 2026-08-09 |
| Phase 6-b bonus_ledger accrual | `post_bonus_accrual()` SECURITY DEFINER server-only: approved period (ADR-006 snapshot-approval boundary — period calculated→approved via `period.manage`) → tek balanced accrual (debit pool=Σfinal / credit accrual per employee) from the completed run's snapshot; idempotent per snapshot; **AD6 gate** (pending_missing_cap_basis → blok); **BL-2** deferred trigger Σaccrual≤pool_ref (AD8-aware); BL-3 → **Phase 6-c done**; snapshot immutable (approval period-level); no new permission — catalog 20; Finance raw-excluded (SI-12) | `migrations/0022`, `seed` (Org C auditor), `tests/0016` | ✅ **verified** | commit `a65013d`; 2026-08-09 |
| Phase 6-c payout/export engine | `produce_payout_export()` SECURITY DEFINER server-only (Finance/trusted server): approved period (AD6 gate 0018 trigger — pending_missing_cap_basis → blok) → `exports` record + period **`approved→exported`**. `mark_payout_paid()` SECURITY DEFINER server-only: exported period → per-employee balanced `payout_marked_paid` (debit accrual / credit payout — doc-06 §2) + **BL-3** DEFERRABLE INITIALLY DEFERRED trigger `enforce_bonus_ledger_payout_cap()` (payout ≤ net accrual, reversal-aware) + period **`exported→closed`**; idempotency guard **BEFORE period gate** (D2 safe). `bonus_ledger`: `payout_marked_paid` unlocked (`export_id NOT NULL` CHECK); `payout_exported` + `clawback_*` hâlâ bloklu. `exports` → `unique(id, organization_id)` kısıtı; `bonus_ledger` → `export_id` same-org composite FK → exports (0026 precedent). Finance views: `v_finance_payout` + `v_finance_period_totals` (security_invoker — raw points/quality/cap_basis/comp **yok** — SI-12). No new permission — catalog 20. 0020 test-only fix: paid-guard fixture `payout_marked_paid`→`payout_exported` (yeni `bonus_ledger_payout_export_chk` CHECK gerektirdi — `payout_exported` yine validate_bonus_ledger_event ile 23514; guard aynı). 20 pgTAP assertion (4 bölüm: A happy-path / B AD6 gate / C ledger guards / D authz+view+cross-tenant) | `migrations/0027`, `tests/0021` (+ `tests/0020` amended) | ✅ **verified** | commit `77615e3`; 2026-08-10 |
| Phase 6-d bonus engine authz hardening | `run_bonus_calculation()` + `post_bonus_accrual()` giriş-authz'ı `CREATE OR REPLACE` ile düzeltildi: `current_user not in ('authenticated','anon')` → **`auth.uid() is null`** (SECURITY DEFINER'da `current_user`=owner olduğundan eski kontrol etkisizdi; `period.manage` fiilen zorlanmıyordu). AD1 uyum; **gövde/mantık/imza/grant değişmedi** (aynı OID → grant+comment korundu); no new permission — catalog 20; regresyon yok (0015/0016 trusted bağlamda geçer) | `migrations/0024`, `tests/0018` | ✅ **verified** | commit `0b8b34a`; 2026-08-09 |
| Phase 7-A anti-gaming detection engine | `run_anti_gaming_scan()` SECURITY DEFINER server-only orkestratör + 4 `detect_*` fonksiyonu (duplicate_task, tiny_task_splitting, same_reviewer_concentration, period_end_spike); `anti_gaming_flags`'a FK-less `bonus_period_id` kolonu + **dual idempotency** partial unique index (OQ-2: task-scoped `related_task_id` / period-scoped `bonus_period_id`); **D5 izolasyon** — yalnız flag yazar, ledger/bonus/comp'a dokunmaz (scan finansal yan-etkisiz); authz `has_role('hr') OR auth.uid() IS NULL`; hardcoded eşikler (OQ-1); no new permission — catalog 20 | `docs/planning/19`, `migrations/0023`, `tests/0017` | ✅ **verified** | commit `ffdea06`; 2026-08-09 |
| Phase 7-B dispute point adjustment | `apply_dispute_point_adjustment()` SECURITY DEFINER server-only: resolved+accepted dispute → mevcut `point_ledger`'a **tek `dispute_adjustment` delta** (employee=complainant); fail-closed 23514 (accepted değilse); idempotent per dispute (partial unique index); non-zero delta (OQ-7B-5); `point_ledger`'a FK-less olmayan `dispute_id` + **same-org composite FK → disputes** (SI-7); event_type CHECK DROP+ADD (aynı isim, 0020 precedent); audit WHEN clause genişletildi (`dispute_adjustment` eklendi, `task_approved` hariç); authz `has_permission('dispute.resolve') OR auth.uid() IS NULL`; D2/D9 ihlali yok; no new permission — catalog 20 | `migrations/0025`, `tests/0019` | ✅ **verified** | commit `70ba400`; 2026-08-09 |
| Phase 7-C dispute bonus recalculation | `recalculate_bonus_after_dispute()` SECURITY DEFINER server-only: completed run → **superseded** (0013 machine); period **`approved→calculated`** (re-approval required — `validate_bonus_period_transition` CREATE OR REPLACE ile bu geçiş eklendi); mevcut `bonus_ledger` accrual'ı **balanced reversal** (debit↔credit swap, yeni transaction_id, append-only); **paid-guard** — accrual satırı paid ise `23514` (D2 clawback-gated); **idempotent** (reversal varsa no-op); authz `has_permission('period.manage') OR auth.uid() IS NULL`; **C-c1 reduced scope** (yeni run/snapshot üretilmez — `run_bonus_calculation` approved period üzerinde çalışamıyor); `dispute_adjustment` → bonus bazına yansıma **Phase 7-D'de giderildi (`31c226f`)**; yeni permission yok — katalog 20; 16 pgTAP assertion | `migrations/0026`, `tests/0020` | ✅ **verified** | commit `8941089`; 2026-08-10 |
| Phase 7-D dispute_adjustment → bonus basis | `point_ledger` + nullable `bonus_period_id` + same-org composite FK → `bonus_periods` (SI-7) + `point_ledger_bonus_period_event_chk` CHECK (`dispute_adjustment ⇒ bonus_period_id NOT NULL`; diğer event'ler ⇒ NULL); `apply_dispute_point_adjustment()` **DROP+CREATE** (imza değişti: +`p_bonus_period_id uuid DEFAULT NULL`; grant re-issue); `run_bonus_calculation()` **CREATE OR REPLACE** (aynı imza → grant/OID korundu): gate `IN('locked','calculated')` (OQ-7D-3; 0015 mesajı birebir korundu); **NET `approved_points` = `task_approved` (tasks.approved_at) + `dispute_adjustment` (bonus_period_id = p_bonus_period_id)** (OQ-7D-1; NULL task_id artık JOIN'de kaybolmaz); net ≤ 0 → `adjusted_score ≤ 0` → dışlanır, 0 prim (OQ-7D-4; D2 malus değil); `bonus_allocations.factors` + `dispute_adjustment_points` kırılımı (OQ-7D-5). 0019 test-only fix (3 satır: #2 positive apply / #7 23505 backstop insert / #12 cross-tenant — yeni CHECK için bonus_period_id eklendi). 15 pgTAP assertion (Section A: basis+net≤0+SI-13+idempotency; B: period-atıf CHECK+FK; C: gate+katalog; D: authz) | `migrations/0028`, `tests/0022` (+ `tests/0019` amended) | ✅ **verified** | commit `31c226f`; 2026-08-10 |
| Phase 7-E dispute bonus re-run orchestration | `recalculate_bonus_after_dispute()` **CREATE OR REPLACE** (aynı 3-arg imza → grant/OID korundu): 7-C C-c1'i tam orkestrasyon'a yükseltir — reversal+supersede+`approved→calculated` (0026 gövdesi korundu) + **YENİ `run_bonus_calculation()` çağrısı** (pool DB'den `SELECT status='locked'`; bulunamazsa `23514`; idempotency key = `'disp-recalc-snap-'` + reversed snapshot id — deterministik, birden fazla dispute döngüsü güvenli); **YENİ snapshot id döner** (7-D engine ile `dispute_adjustment` bazda yansır). Period `'calculated'` **kalır** — HR ayrıca `approve` → `post_bonus_accrual()` (ADR-006 human re-approval korundu). **İdempotent**: reversal+run → yeni snap; reversal var/run yok → pool-fetch+run'a düşer; her ikisi varsa → yeni snap (no-op). 0020 test-only fix (3 assertion: `_b7c_snap` helper accrual-filtreli; `#6` supersede scope; `#10` yeni re-run snapshot). 14 pgTAP assertion (A: 8 full orchestration / B: 3 idempotency / C: re-accrual gated / D: pool guard / E: precondition / F: authz+katalog 20) | `migrations/0029`, `tests/0023` (+ `tests/0020` amended) | ✅ **verified** | commit `3efe95d`; 2026-08-10 |
| Phase 5-b manual point override | `apply_manual_point_adjustment()` — two-person point override (caller + `p_actor` + `p_second_approver` all hold `point.override`, same org; distinct approvers; non-zero delta; non-empty reason; employee + optional task same-org) → tek `manual_adjustment` ledger satırı; mevcut audit trigger'ı yazar; **şema değişikliği yok** | `migrations/0030`, `tests/0024` | ✅ **verified** | commit `b719053`; 17 assertion; caller + dual-actor point.override; cross-tenant block; append-only; schema değişikliği yok |
| Phase 8 UI layer | Next.js App Router dashboards & UX layer — **8-A** 5 Zod schema (`src/lib/validation/schemas/`), **8-B** 12 server action (`src/app/actions/`), **8-C** 35 sayfa+feature component, **8-D** nav activation (`src/components/app-nav.tsx`), **8-E** dashboard role summary; server-side `requirePermission`/RLS; Finance view'ları `createClient` ile; app UI layer — migration yok / test yok | app UI layer — migration yok · test yok (—) | ✅ **verified (tsc)** | commit `7f51cfd`; Files=54; **tsc clean** (`npx tsc --noEmit`); 8-A..8-E ✓; DB değişikliği yok. Bilinen sınır: leaderboard employee view = own-standing only (RLS) |
| Phase 9 app-layer test suite | **9-A** 38 Zod schema tests, **9-B** 43 server action tests, **9-C** 4 security-boundary invariants, **9-D** 10 E2E route-guard specs (Playwright, env-gated); 11 Vitest test files / 98 tests; no migration / no DB change | `src/` test files (8 new) | ✅ **Vitest pass; E2E authored env-gated** | commit `cae487a`; 8 files / 98 tests (11 files total); 9-A schemas + 9-B actions + 9-C boundary + 9-D E2E smoke; Vitest pass; E2E authored env-gated; no migration/DB change |
| Phase 10 production readiness | Sentry DSN-gated instrumentation hook (`src/instrumentation.ts`); audit CSV export with AD3 comp masking; support access workflow UI (D4 time-bounded grants); nav item added; no migration / no DB change | 12 files / 634 insertions | ✅ **tsc clean + vitest 98/98 PASS** | commit `0403811`; instrumentation hook (Decision C, scaffolded — no-op until @sentry/nextjs supports Next 16); audit export (AD3 masking); support access workflow (D4); nav; tsc clean; no migration/DB change |
| Phase P0 shared intelligence foundation | Product Intelligence foundation (plan §2, ADR-020-authorized): **semantic metric SSOT** (`metrics/` — MetricId/MetricResult/registry/validator; **contract-only, no business SQL**), **dimension catalog** (`dimensions/`; person-level dims gated by `intelligence.manage`), **evidence envelope + insight lifecycle** (`domain/`; evidence+action invariant, §2.8 status machine), **insight store** `intelligence_insights` + `IntelligenceRepository` (RLS reads / **server-only writes**; deterministic_payload+evidence AUTHORITATIVE, model_payload advisory/nullable — null in P0), **feature flags** `feature_flags` + `FeatureFlagResolver` (default off, tenant-scoped, **manage-gated writes**), **telemetry** emitter (typed, routes to existing Sentry seam, **DSN-safe no-op**), **viz primitives** (`src/components/intelligence/` — MetricCard/DeltaBadge/DriverList/EvidenceDrawer/InsightCard; accessible; Recharts **deferred to P4**); **permissions `intelligence.read`/`intelligence.manage` — catalog 20 → 22**; module registered; **NO LLM / NO financial calc / NO ledger** | `migrations/0042`, `tests/0041`, `src/modules/intelligence/*`, `src/components/intelligence/*`, `tests/unit/modules/intelligence/*` (8), seed + `db-surface.yml` + manifest + README | ✅ **gates green** | INTEL-P0; RLS ENABLE+FORCE on both new tables; **pgTAP `0041` 32/32** — cross-tenant isolation + employee-own read + server-only insight writes + manage-gated flag writes + least-priv grants + evidence/action CHECK; **`db reset` 0001..0042 + seed clean; `supabase test db` Files=36 Tests=928 PASS** (12 existing count assertions 20→22); **verify:fast** (lint+tsc+unit 468 + madge no-cycle + manifest 22); **verify:contracts** OK; type-drift none; live SECURITY DEFINER audit 53/53 (0 added); vitest 8 files/49 tests (validator/schemas/registry/lifecycle/feature-flags/telemetry/viz/repository). NOTE: full `verify:engineering` wrapper not run locally — its `sbom` step's `mkdir -p artifacts` fails under Windows cmd.exe (OS quirk, green on Linux CI); DB steps validated via Postgres-only stack |
| Phase P1 6-A policy change diff + governance (Module 6-A) | Policy Change Impact governance (plan §8; base origin/main with P0 merged): **structural diff engine** (`domain/diff.ts` — deterministic, order-independent, metadata-ignoring; §8.3/§8.11) + **change-request state machine** mirror; **governance table** `policy_change_requests` (migration 0043; RLS ENABLE+FORCE; state machine + **dual HR+Finance approval** role-gated/one-time/self-stamped + auto-approve; **retroactive effective_date blocked** §8.11; **published version NEVER mutated** §8.2/§26; `log_audit`) + `PolicyChangeRequestRepository` + feature-flag-gated application (create/submit/approve-HR/approve-Finance/reject/request-changes/computeDiff); **permission `policy.impact.read` — catalog 22 → 23**; module registered; **NO backtest/scoring-mirror/impact-ledger/insight/UI (6-B/6-C); NO ledger; NO published mutation; NO LLM** | `migrations/0043`, `tests/0042`, `src/modules/policy-change-impact/*`, `tests/unit/modules/policy-change-impact/*` (2), seed + `db-surface.yml` + `access-matrix.yml` + allowlist + manifest + 13 catalog assertions 22→23 + n1-drill retargeted to 0043 | 🟡 non-DB gates green (DB on CI) | INTEL-P1-6A; **verify:fast** (lint + tsc + **unit 507** + madge no-cycle + **manifest 23 modules**) + **verify:contracts** OK (43 migrations linted, surface classified). pgTAP `0042` authored (RLS ENABLE+FORCE + cross-tenant BLOCKING + read gated by policy.impact.read + create gated by policy.manage + dual-approval role gating + approval audited + published-immutable §8.2 + retroactive §8.11 + catalog=23). **DB gates deferred (Docker Desktop down locally)**: `db reset` / `supabase test db` / `npm run db:types` (regenerate `database.generated.ts` for the new table — required by verify:db drift) run on Linux CI. Pre-flight D backtest decision = Option (a) pure scoring mirror, lands in 6-B |
| Phase P1 6-B impact backtest + immutable ledger + insight (Module 6-B) | Policy Change Impact backtest (plan §8.4/§8.5/§8.8; base origin/main w/ 6-A merged): **pure scoring mirror** (`domain/scoring-mirror.ts` — reproduces the 0020 `compute_final_points` formula EXACTLY, raw numeric, AD5) + **deterministic backtest engine** (`domain/backtest.ts` — re-score a frozen reference period's approved work under from vs to_draft version, **reuse `allocateBonus`** with identical PoolConfig, §8.5 summary: affected/higher/lower/unchanged/budget/median/max-negative) + **`policy_change_impacts`** append-only IMMUTABLE artifact (migration 0044; RLS ENABLE+FORCE; **server-only writes**; UPDATE/DELETE blocked via `prevent_mutation`; `log_audit`; same-org composite FKs; health/complexity_delta **NULL** deferred §8.6) + `PolicyChangeImpactRepository` + `generatePolicyChangeImpact` (feature-flag + `policy.manage` gated; **admin client injected** — SI-11 boundary; emits a `policy_change_impact` **P0 Insight** via `IntelligenceRepository` with evidence+actions). **NO ledger write; NO `run_bonus_calculation`; NO published-version mutation; NO LLM.** Catalog unchanged (23) | `migrations/0044`, `tests/0043`, `src/modules/policy-change-impact/{domain/scoring-mirror,domain/backtest,repository/policy-change-impact-repository,application/generate-impact}.ts`, `tests/unit/modules/policy-change-impact/{scoring-mirror,backtest}.test.ts`, seed + `db-surface.yml` + `access-matrix.yml` + manifest (+publicApi) + manifest-test 38→39 | 🟡 non-DB gates green (DB on CI) | INTEL-P1-6B; **verify:fast** (lint + tsc + **unit 523** + madge no-cycle + **manifest 23 modules**) + **verify:contracts** OK (44 migrations linted, surface classified). **Scoring-mirror parity proven**: unit golden (base 1000→1000; base 100 medium/high→187.5; penalty/cap) + backtest **from-side reproduces doc-05 §8** (Ali 3,177,630 / Ayşe 3,177,629 / Mehmet 953,289 / Zeynep 2,691,452); pgTAP `0043` anchors it to the DB (`point_ledger` Ali task 210 = 1000 + `compute_final_points`). pgTAP `0043` authored (RLS ENABLE+FORCE + cross-tenant BLOCKING + read gated policy.impact.read + immutable UPDATE/DELETE + audited + same-org FK + catalog 23). **DB gates deferred (Docker down locally)**: `db reset` / `supabase test db` / regen `database.generated.ts` for the new table (download CI artifact `database-generated-types`) run on Linux CI. Deferred: health/complexity deltas (P2/later), 6-C UI |
| Phase P1 6-C policy change impact UI (Module 6-C) | App-layer UI for Module 6 (plan §8.9/§8.10/§18/§19; base origin/main w/ 6-B merged): **list + detail routes** `app/(app)/policy-impact/{page,[id]/page,loading}.tsx` — side-by-side structural diff (`computePolicyDiff` → DriverList/table), §8.5 impact summary (MetricCard/DeltaBadge), affected cohorts (table-first, no raw comp), advisory **InsightCard + EvidenceDrawer**, deterministic **communication preview** (§8.9, NO LLM), and CTAs. **5 server actions** `src/app/actions/policy-impact/*` (create/submit/approve/decide/run-simulation) — thin, Zod-validated, **server-side requirePermission + role gate**, delegate to the module public API; run-simulation injects the admin client (SI-11). **Nav** entry gated on `policy.impact.read` + the **`policy_change_impact` feature flag** (layout resolves flags → AppNav); page redirects `/unauthorized` when the permission is missing or the flag is off. **NO DB/migration/permission change; NO Recharts (waterfall deferred → table-first); imports the module only via `@/modules/policy-change-impact`, viz only via `@/components/intelligence`.** Empty/loading/error states; accessible (WCAG AA, table alternatives, no color-only) | `app/(app)/policy-impact/*` (3), `src/app/actions/policy-impact/*` (5), `src/lib/validation/schemas/policy-impact.ts`, `src/components/features/policy-impact/*` (8), `app/(app)/layout.tsx` + `src/components/app-nav.tsx` (flag-gated nav), `tests/unit/actions/policy-impact.test.ts` | ✅ **gates green** | INTEL-P1-6C; **verify:fast** (lint + tsc + **unit 540** + madge no-cycle + **manifest 23**) + **verify:contracts** OK. App-layer tests: 3 schema + 12 action gating/deny + **security-boundary** (employee/manager cannot approve/decide; every action calls requirePermission; no client imports admin). Authz per surface: view=policy.impact.read; create/submit/run=policy.manage; approve/decide=policy.impact.read floor + HR/Finance role; all rely on RLS + the validate trigger. NOTE: full-suite unit runs intermittently hit the Windows vitest worker-startup flake — each step confirmed green standalone (boundary 7/7, unit 540/540). NO DB change → no type-drift. E2E deferred to Linux CI |
| Phase P1 4-A policy complexity static engine + evaluation store (Module 4-A) | Policy Debt / Complexity Meter first slice (plan §6.2/§6.3/§6.4/§6.9; §3.5 versioned rules; §26 no-mutation gate; base origin/main w/ Module 6 merged): **deterministic versioned static-complexity engine** (`domain/complexity-rules.ts`, `RULE_SET_VERSION='static-v1'`) — reads a `scoring_policy_version` config (READ-ONLY, §26) and computes a static score decomposed into **transparent per-component drivers** (dimension_count, bucket_count, threshold_cliffs, revision_penalty, timeliness_threshold_count); **no opaque aggregate** (score = Σ driver impacts), no NaN, reproducible (same config + rule_set_version → identical result). **`policy_complexity_evaluations`** append-only IMMUTABLE store (migration 0045; RLS ENABLE+FORCE; **server-only writes**; UPDATE/DELETE blocked via `prevent_mutation`; `log_audit`; same-org composite FK → scoring_policy_versions; **unique(policy_version_id, rule_set_version)**; runtime_score NULL / total=static this slice — §6.5/§6.6 are 4-B) + `PolicyComplexityRepository` + `evaluatePolicyComplexity` (feature-flag `policy_debt` gated; idempotent; admin client injected — SI-11). **NO policy mutation; NO opaque score; NO LLM.** Reuses `policy.manage` (read) — catalog unchanged (23); no new SECURITY DEFINER fn | `migrations/0045`, `tests/0044`, `src/modules/policy-complexity/{domain/complexity-rules,domain/types,repository/policy-complexity-repository,application/evaluate-complexity,application/feature-gate,index}.ts`, `tests/unit/modules/policy-complexity/complexity-rules.test.ts`, seed + `db-surface.yml` + `access-matrix.yml` + allowlist(range) + manifest (+module) + manifest-test 39→40 | ✅ **non-DB gates green** | INTEL-P1-4A; **verify:fast** (lint + tsc + **unit 550** + madge no-cycle + **manifest 24 modules**) + **verify:contracts** OK (45 migrations linted, surface classified). Engine unit/property tests: golden d2 config = **static 32** with driver breakdown (dim 4×2 + buckets 16×1 + 1 cliff ×5 + penalty ×3); simple < rich; cliff detection (≥2× ratio + zero-bucket); reproducibility; monotonicity (adding a dimension never lowers the score); no-NaN property. pgTAP `0044` authored (RLS ENABLE+FORCE + cross-tenant BLOCKING + read gated policy.manage + server-only + immutable UPDATE/DELETE + audited + same-org FK + unique(version,rule_set) + catalog 23). **DB gates deferred (Docker down locally)**: `db reset` / `supabase test db` / regen `database.generated.ts` for the new table (download CI artifact `database-generated-types`) run on Linux CI. Computed §6.2 components: dimension/bucket count, threshold cliffs, penalty presence, custom timeliness thresholds. Deferred (not in scoring_policy_versions schema): role variants, exceptions, special/temporary/legacy rules, external deps, policy depth; runtime complexity / debt total / trend / simplification / UI = 4-B |
| Phase P1 4-B policy debt runtime + trend + simplification insights (Module 4-B) | Policy Debt second slice (plan §6.5/§6.6/§6.7/§6.8; §26 gate; base origin/main w/ 4-A merged; NO new migration — reuses 0045): **deterministic versioned runtime engine** (`domain/runtime-rules.ts`, `runtime-v1`) — transparent drivers override_usage + recalculation (+ scored-volume context); **debt total** (`domain/debt.ts`, `FULL_RULE_SET_VERSION='full-v1'`) = static + runtime, persisted as a NEW immutable 0045 row (bumped rule_set_version; 4-A `static-v1` rows untouched; `unique(version, rule_set)` respected; no UPDATE); **version trend** (`domain/trend.ts`) with per-version deltas; **advisory simplification candidates** (`domain/simplification.ts`) — duplicate-outcome + unused-bucket, **NEVER auto-applied (§26)** — emitted as P0 `policy_debt` **Insights** (evidence+actions "Kuralı incele"/"Politika taslağı oluştur"). Repo extended (runtime-signal reads via point_ledger→tasks join + superseded runs; bucket usage; versions-for-policy) + `evaluatePolicyDebt` (idempotent, admin injected — SI-11, emit-once) + `getPolicyComplexityTrend` (RLS user client). All behind the `policy_debt` flag. **NO policy mutation; NO opaque score; NO auto-rewrite; NO LLM.** No new DB surface → catalog unchanged (23), no type-drift | `src/modules/policy-complexity/{domain/runtime-rules,domain/debt,domain/trend,domain/simplification}.ts` (new), `{application/evaluate-debt,application/version-trend}.ts` (new), `domain/types.ts` + `repository/policy-complexity-repository.ts` + `index.ts` + `module-manifest.json` (extended), `tests/unit/modules/policy-complexity/runtime-debt.test.ts` | ✅ **gates green** | INTEL-P1-4B; **verify:fast** (lint + tsc + **unit 562** + madge no-cycle + **manifest 24**) + **verify:contracts** OK (no new migration). Unit/property tests: runtime golden (2×3+1×4=10 with drivers), reproducibility, no-NaN, monotonicity; debt=static+runtime; trend deltas [0,+12,−2]; simplification duplicate/unused golden + no-false-positive. **Adversarial multi-agent review** (workflow) confirmed 1 HIGH — dispute-adjustment signal was mis-attributed via the always-null point_ledger.scoring_policy_version_id → **removed the dead signal** (deferred: needs a disputes.target_id→tasks→version resolver). No new DB surface → NO type-drift, no pgTAP. Deferred (§6.5): dispute-adjustment usage, raw disputes.count, anti_gaming_flags, admin-effort; runtime override attribution = task-linked manual_adjustments only |
| Phase P1 4-C policy debt UI (Module 4-C) | Policy Debt / Complexity Meter **UI** — final slice of Module 4 & Phase P1 (plan §6.10; §18 UX; §26 gate; **APP-LAYER ONLY** — base origin/main w/ 4-B merged; **NO migration, NO DB/schema change, NO pgTAP, NO new permission** — reuses `policy.manage`): **list page** (`/policy-debt`) — evaluated scoring policies with latest debt total (RLS-scoped reads of scoring_policies + scoring_policy_versions + `PolicyComplexityRepository.list`); **detail page** (`/policy-debt/[policyId]`) — complexity/**DEBT badge** + Δ-from-previous (MetricCard, higher-is-worse), **version trend** (table-first, `getPolicyComplexityTrend`; statik/tam labelled), **highest complexity drivers** (DriverList over the latest full-v1 evaluation's transparent components — no opaque score), **advisory simplification candidates** (table + explicit "danışma amaçlı; otomatik uygulanmaz" — **NO auto-apply control, §26**) + the `policy_debt` **Insight** (InsightCard: evidence+actions), **"Şimdi değerlendir" CTA** (client `EvaluateButton` → server action). **Server action** `evaluatePolicyDebtAction` (`validatedAction` + Zod uuid schema) — `requirePermission('policy.manage')` server-side then delegates to `evaluatePolicyDebt` (idempotent; admin client created HERE & injected — SI-11); returns {evaluationId, totalScore, candidateCount}. Every page gates server-side on `hasPermission('policy.manage')` **AND** `FeatureFlagResolver.isEnabled('policy_debt')` (→ /unauthorized); **client-side authz is never source of truth (AD1)** — the CTA re-checks server-side. Nav "Politika Borcu" (Gauge; management; `policy.manage` + `policy_debt` flag) + layout resolves the flag. **NO policy mutation; NO auto-apply; NO LLM; NO Recharts (table-first); no deep module imports.** No new DB surface → catalog unchanged, no type-drift | `app/(app)/policy-debt/{page,loading}.tsx` + `[policyId]/page.tsx`, `src/app/actions/policy-debt/evaluate-policy-debt.ts`, `src/lib/validation/schemas/policy-debt.ts`, `src/components/features/policy-debt/{policy-list,debt-trend,simplification-candidates,evaluate-button}.tsx`, `src/components/app-nav.tsx` (+Gauge nav item) + `app/(app)/layout.tsx` (resolve `policy_debt` flag), `tests/unit/actions/policy-debt.test.ts` | ✅ **gates green** | INTEL-P1-4C; **verify:fast** (lint + tsc + **unit 568** + madge no-cycle + **manifest 24**) + **verify:contracts** OK (no new migration). Tests: schema (uuid required, commandId optional, rejects non-uuid); action happy (enforces `policy.manage` + delegates ctx + injected admin) / deny (requirePermission throws → module NOT called) / BOUNDARY (employee denied) / validation (bad uuid → module NOT called); auto-covered by `security-boundary` (new action has `requirePermission(`; client component imports no admin). App-layer only → no pgTAP/DB gate needed. Concludes Phase P1 Module 4 |
| Phase P2 1-A incentive health rule engine + evaluation store (Module 1-A) | Incentive Health Engine first slice (implementation_product_9_modules §3.2–3.6/§3.9/§12.4; §26 gate: **NO OPAQUE SCORE**; base fresh origin/main w/ P1 merged): **deterministic, versioned health rule engine** (`src/modules/incentive-health/domain/rules/**`, `HEALTH_RULE_SET_VERSION='health-v1'`) — **six computed dimensions** each returning `{ score(0–100), confidence, drivers[], evidence[] }` (§3.4): **financial_integrity** (pool conservation + missing-cap-basis), **payout_concentration** (top-10% share + Gini + mean/median divergence), **gaming_resistance** (config threshold-cliffs, reuses Module 4 cliff logic), **manager_discretion** (task-linked manual-override share), **dispute_exposure** (polymorphic-target attributable disputes), **complexity** (Module 4 static score normalized, §12.4). Thresholds live in **versioned rule modules** (§3.5: id/version/rationale). **overall_score = published weighted mean of the sub-scores** (`weights.rules.ts`; weights persisted) — **transparent, re-derivable, NEVER opaque (§26)**; reproducible (same signals + rule_set → identical), 0–100, no NaN, monotonic risk. Deferred + documented: **opportunity_balance** (Module 2 unbuilt), **controllability** (no clean source), operational anti-gaming sub-signals (no version FK), task-less manual overrides (no version linkage). **Migration 0046** `policy_health_evaluations` (overall_score + dimensions jsonb; RLS ENABLE+FORCE; server-only writes; read `policy.manage`; append-only IMMUTABLE via `prevent_mutation`; same-org composite FK → scoring_policy_versions; unique(policy_version_id, rule_set_version); 0–100 CHECK; `log_audit`; adds NO definer). **Repository** (RLS reads / admin writes) reads the frozen signals via the deterministic version join-paths + **evaluate-and-persist** `evaluatePolicyHealth` (feature-flag `health_engine` gated; idempotent per (version, rule_set); emits an advisory `policy_health` P0 Insight). **NO policy mutation; NO ledger write; NO run_bonus_calculation; reads only; NO LLM.** Reuses `policy.manage` → **catalog stays 23** | `src/modules/incentive-health/{domain/types,domain/score-util,domain/evaluate-health,domain/rules/{concentration,discretion,gaming,dispute,financial-integrity,complexity,weights}.rules,repository/incentive-health-repository,application/{feature-gate,evaluate-and-persist},index}.ts`, `migrations/0046`, `tests/0045` (pgTAP), seed fixtures, `tests/unit/modules/incentive-health/health-engine.test.ts`, `module-manifest.json`(+README) + `db-surface.yml` + `access-matrix.yml` + `db-surface-manifest.test.ts` (40→41 tables) | ✅ **non-DB gates green** | INTEL-P2-1A; **verify:fast** (lint + tsc + **unit 585** + madge no-cycle + **manifest 25 modules**) + **verify:contracts** OK (46 migrations linted, surface classified). Unit/property: each dimension at exact threshold boundaries; transparent-overall = weighted mean (no opaque score); reproducibility; range/no-NaN/monotonic-risk (fast-check); rule catalogs expose id/version/rationale. **Adversarial multi-agent review** (workflow, 11 agents) confirmed 3 findings, all fixed: removed the **dead `CAP_EXCEEDED`** signal (DB CHECK makes cap-overflow impossible ⇒ always 0 — cf. 4-B dead-signal removal); **concentration now counts zero payouts** (`v>=0`) so the have-nots aren't dropped (+regression test); **documented** the task-linked-only Manager Discretion coverage limit. pgTAP `0045` authored (RLS ENABLE+FORCE + cross-tenant BLOCKING + read gated `policy.manage` + server-only + immutable UPDATE/DELETE + audited + same-org FK + unique + 0–100 range + **no-opaque-score re-derivation** + catalog 23). **DB gates deferred (Docker down locally)**: `db reset` / `supabase test db` / regen `database.generated.ts` for 0046 (download CI artifact `database-generated-types`) run on Linux CI. First slice of Module 1 & Phase P2; risk-acceptance (1-B), policy comparison (1-B), UI (1-C) NOT built |
| Phase P2 1-B health risk-acceptance workflow + policy comparison (Module 1-B) | Incentive Health second slice (implementation_product_9_modules §3.4/§3.8/§3.11, §2.7 audit; §26 gate; base fresh origin/main w/ 1-A merged): **risk-acceptance (waiver) governance record** + **comparison-to-previous health delta**. Extends `@/modules/incentive-health`. **Migration 0047** `policy_health_risk_acceptances` (organization_id, policy_version_id, health_evaluation_id, dimension, driver_code, reason, accepted_by, accepted_at, expires_at): RLS ENABLE+FORCE; **AUTHENTICATED INSERT** gated by `WITH CHECK (org=current_org() AND has_permission('policy.manage') AND accepted_by=auth.uid())` (mirrors the 0043 governance-insert precedent); read `policy.manage`; grants `select,insert` only (**NO update/delete** — append-only via `prevent_mutation`); reason/dimension/driver non-empty CHECK; **expiry sanity** CHECK (`expires_at IS NULL OR > accepted_at`); `accepted_at` server-stamped; **`log_audit` on INSERT** (§2.7 policy risk waiver); same-org composite FKs to scoring_policy_versions + memberships; **3-column FK** `(health_evaluation_id, org, policy_version_id) → policy_health_evaluations` (DB-enforces the waiver↔evaluation↔version linkage — a driver can't be mis-attributed to another version). Also ALTERs 0046 to add `unique(id, organization_id, policy_version_id)` (net-new). Adds NO SECURITY DEFINER function. **Risk-acceptance application** `acceptHealthRisk` (validates the (dimension, driver_code) exists in the referenced evaluation, **derives policy_version_id from the evaluation** so it can't be spoofed, requires non-empty reason; writes via the RLS user client so auth.uid()=accepted_by) + `listHealthRiskAcceptances`; both `health_engine` flag-gated. **Comparison-to-previous** (`domain/compare-health.ts`, PURE + deterministic): `computeHealthComparison` (overall + per-dimension deltas, higher=healthier ⇒ +delta=improvement; first version → null deltas; missing dimension → null, no NaN) + `pickPreviousVersion` (largest version_no strictly below, gap-tolerant) + `getHealthComparison` application read. **A risk is NEVER silently dismissed (§3.8)** — only accept-with-reason+actor+timestamp+optional-expiry, audited. **Accepting a waiver NEVER changes the computed health score and mutates NO policy/ledger (§26/AD7); NO LLM.** **Revocation OUT of scope** (a waiver lapses via expires_at; explicit revocation deferred). Reuses `policy.manage` → **catalog stays 23** | `src/modules/incentive-health/{domain/compare-health,application/risk-acceptance,application/health-comparison}.ts` (new) + `{repository/incentive-health-repository,index}.ts` (extended), `migrations/0047` (+ALTER 0046), `tests/0046` (pgTAP), seed fixtures, `tests/unit/modules/incentive-health/risk-comparison.test.ts`, `module-manifest.json` + `db-surface.yml` + `access-matrix.yml` + `db-surface-manifest.test.ts` (41→42 tables) | ✅ **non-DB gates green** | INTEL-P2-1B; **verify:fast** (lint + tsc + **unit 592** + madge no-cycle + **manifest 25 modules**) + **verify:contracts** OK (47 migrations linted, surface classified). Unit: comparison overall + per-dimension deltas (sign, alignment, first-version→null, absent-dimension→null, reproducibility); previous-version selection (strictly-below, gap-tolerant, self/future-excluded, empty→null). **Adversarial multi-agent review** (workflow, 11 agents) confirmed 1 finding, fixed: the waiver's policy_version_id vs the referenced evaluation's version was DB-unlinked (app-derived only) → added the **3-column composite FK** so the linkage is DB-enforced (+ pgTAP mismatch test). pgTAP `0046` authored (RLS ENABLE+FORCE + cross-tenant BLOCKING + **authenticated-INSERT gating** policy.manage+self else 42501 + reason non-empty + expiry sanity + append-only UPDATE/DELETE blocked + audited + same-org FKs + version-linkage FK + catalog 23). **DB gates deferred (Docker down locally)**: `db reset` / `supabase test db` / regen `database.generated.ts` for 0047 (download CI artifact `database-generated-types`) run on Linux CI. Second slice of Module 1; UI (1-C) NOT built; revocation deferred |
| Phase P2 1-C policy health UI (Module 1-C) | Incentive Health **UI** — final slice of Module 1 (implementation_product_9_modules §3.3/§3.4/§3.7/§3.8, §18/§19 UX, §26 gate; **APP-LAYER ONLY** — base fresh origin/main w/ 1-B merged; **NO migration, NO DB/schema change, NO new permission** — reuses `policy.manage`): **list page** (`/policy-health`) — scoring policies + latest overall (RLS reads of scoring_policies + scoring_policy_versions + `IncentiveHealthRepository.list`, filtered to `HEALTH_RULE_SET_VERSION`); **detail page** (`/policy-health/[policyId]`) — **header** (version + overall/100 + N issues), **dimension cards** (MetricCard + DeltaBadge per dimension, Δ vs previous via `getHealthComparison`; deferred Opportunity Balance/Controllability shown as "not evaluated", never fake 0), **transparent overall derivation** (weighted-aggregate table from persisted weights — **NO opaque score, §26**), **risk list** (DriverList + EvidenceDrawer; **EVERY negative-impact driver is a reviewable risk** — none silently hidden, §3.8) + **Accept Risk** (reason REQUIRED + optional future expiry → audited 1-B workflow), **comparison-to-previous** (table-first), CTAs (Evaluate/refresh; Run simulation / Create draft **disabled** — Module 6/7 out of scope). **Server actions**: `evaluatePolicyHealthAction` (admin client injected, SI-11; idempotent) + `acceptHealthRiskAction` (**RLS user client**, NOT admin — preserves the 1-B authenticated-INSERT `WITH CHECK accepted_by=auth.uid()` + a non-null `log_audit` actor for §2.7); both `validatedAction` + Zod + **server-side `requirePermission('policy.manage')`**. Every page gates server-side on `hasPermission('policy.manage')` **AND** `FeatureFlagResolver.isEnabled('health_engine')` (→ /unauthorized); **client-side authz is never source of truth (AD1)**. Nav "Politika Sağlığı" (HeartPulse; management; `policy.manage` + `health_engine` flag) + layout resolves the flag. **NO opaque score; a risk is never silently dismissed (only Accept-Risk with reason, audited); NO policy/ledger/score mutation; NO LLM; NO Recharts (table-first); no deep imports (module via `@/modules/incentive-health`, viz via `@/components/intelligence`).** No DB change → no type-drift | `app/(app)/policy-health/{page,loading}.tsx` + `[policyId]/page.tsx`, `src/app/actions/policy-health/{evaluate-health,accept-risk}.ts`, `src/lib/validation/schemas/policy-health.ts`, `src/components/features/policy-health/{view,policy-list,health-header,dimension-cards,overall-derivation,risk-list,comparison-table,accept-risk-button,evaluate-button}.{ts,tsx}`, `src/components/app-nav.tsx` (+HeartPulse nav item) + `app/(app)/layout.tsx` (resolve `health_engine`), `tests/unit/actions/policy-health.test.ts` + `tests/unit/components/policy-health-view.test.ts` | ✅ **gates green** | INTEL-P2-1C; **verify:fast** (lint + tsc + **unit 610** + madge no-cycle + **manifest 25**) + **verify:contracts** OK (no new migration). Tests: action schemas; evaluate/accept happy (correct client — admin vs RLS-user — + delegates ctx) / deny (requirePermission throws → module NOT called) / BOUNDARY (employee denied) / validation (bad uuid, blank reason → module NOT called); view helpers (health bands, fail-closed expiry, every negative driver surfaced §3.8, issues count); auto-covered by `security-boundary`. **Adversarial multi-agent review** (workflow, 13 agents) confirmed **6 findings, all fixed**: (§3.8) **negative-impact drivers in a healthy dimension were filtered out of review → now every negative driver is a reviewable warning** (no silent dismiss); list-page versions-read error swallowed → now fails to ErrorState; `isActiveAcceptance` fail-open on corrupt expiry → **fail-closed**; missing client future-expiry guard → added; list overall not filtered by rule-set → filtered; raw English status → localized badge. App-layer only → no pgTAP/DB gate. **Concludes Module 1 & the Incentive Health Engine** |
| Phase P3 2-A opportunity index + snapshot engine (Module 2-A) | Opportunity-to-Perform Intelligence first slice (implementation_product_9_modules §4.2/§4.3/§4.4/§4.5/§4.6/§4.10, §17 small-cohort privacy, §26 gate; base fresh origin/main w/ P2 merged): **deterministic, versioned, DECOMPOSED opportunity engine** (`src/modules/opportunity-intelligence/domain/**`, `OPPORTUNITY_RULE_SET_VERSION='opportunity-v1'`) — **five components** each with transparent drivers (work_availability, assignment_share, complexity_exposure, review_throughput, active_period); the index is a **published weighted aggregate** of the sub-scores (re-derivable — **NO opaque ML, §26**). **Cohort-normalized** (§4.5): same `primary_role` (memberships) + same **PRIMARY team** (`team_memberships.is_primary`, AD9) + same period; per-active-day rates (fair for part-time/new-hire/leave); **MIN_COHORT_SIZE=5** → a smaller cohort (or no primary team / no active window) is **SUPPRESSED** (index null, `COHORT_SUPPRESSED`) — never a misleading score / raw-count comparison (§17). Uses **WORK-CONTEXT SIGNALS ONLY** — **NO protected characteristic and NO compensation is read ANYWHERE** (§4.2/§26; confirmed none exist on any table). **Five advisory flags** (§4.6, deterministic, "Investigate", never a verdict): LOW_ASSIGNMENT_OPPORTUNITY, HIGH_COMPLEXITY_IMBALANCE (bidirectional mix-balance), REVIEW_BOTTLENECK, LOW_ELIGIBLE_WORK_AVAILABILITY, PERFORMANCE_OPPORTUNITY_MISMATCH. **Migration 0048** `opportunity_snapshots` (§4.4; RLS ENABLE+FORCE; **three-tier read cloned from point_ledger** — employee-own + manager of the employee's PRIMARY team via `manages_team(team_of(employee_id))` [**no cross-team leakage**] + HR/owner/admin/auditor + support-grant; server-only writes; append-only IMMUTABLE via `prevent_mutation`; same-org composite FKs → memberships + bonus_periods; unique(employee_id, bonus_period_id, rule_set_version); 0–100/null index + count CHECKs; `log_audit`; **NO protected/compensation columns**; adds NO definer). **Repository** reads work-context signals only (period-scoped tasks + reviews + eligibility + memberships) → frozen MemberSignals; **compute-and-persist** `computeOpportunitySnapshots` (feature-flag `opportunity_intelligence`; idempotent per (employee, period, rule_set); emits advisory `opportunity_flag` insights). **NO automatic pay adjustment; NO opaque ML; NO surveillance; NO LLM.** Role-based RLS → **catalog stays 23** | `src/modules/opportunity-intelligence/{domain/types,domain/score-util,domain/compute-opportunity,domain/rules/{weights,flags}.rules,repository/opportunity-repository,application/{feature-gate,compute-and-persist},index}.ts`, `migrations/0048`, `tests/0047` (pgTAP), seed fixtures, `tests/unit/modules/opportunity-intelligence/opportunity-engine.test.ts`, `module-manifest.json` + `db-surface.yml` + `access-matrix.yml` + `db-surface-manifest.test.ts` (42→43 tables) | ✅ **non-DB gates green** | INTEL-P3-2A; **verify:fast** (lint + tsc + **unit 623** + madge no-cycle + **manifest 26 modules**) + **verify:contracts** OK (48 migrations linted, surface classified). Unit/property: cohort suppression (< min sample / no team / no active period); no-cross-team cohort keying; part-time active-days normalization; bidirectional complexity imbalance; review-bottleneck; performance-opportunity mismatch; range 0–100/null + no-NaN + reproducibility (fast-check); **explicit "no protected attribute" assertion** on MemberSignals. **Adversarial multi-agent review** (workflow) confirmed 4 findings, all fixed: (HIGH×2) seed + pgTAP used a compensation-record UUID (`…c1`) as `bonus_period_id` → would 23503-abort `db reset`/tests → repointed to real periods (`a0..fa`/`b0..fa`, cross-org FK case now uses a genuine Org B period); (MED) tasks date filter dropped the period's last day → half-open `[starts_on, ends_on+1d)`; (MED) insight `subjectType 'membership'` → `'employee'` so the intelligence RLS employee-own branch matches. pgTAP `0047` authored (RLS ENABLE+FORCE + cross-tenant BLOCKING + **three-tier reads incl. NO cross-team leakage** [manager a5 sees a7 not a8] + finance sees none + server-only + immutable UPDATE/DELETE + audited + same-org FKs + unique + catalog 23). **DB gates deferred (Docker down locally)**: `db reset` / `supabase test db` / regen `database.generated.ts` for 0048 (download CI artifact `database-generated-types`) run on Linux CI. First slice of Module 2 & Phase P3; UI / scatter / manager view / exception routing = 2-B (not built) |
| Phase P3 2-B opportunity UI + flag review (Module 2-B) | Opportunity-to-Perform Intelligence **UI** — final slice of Module 2 & Phase P3 (implementation_product_9_modules §4.8 scatter/quadrant + manager view, §4.9 advisory flag review, §18/§19 UX, §2.7 audit, §2.8 status lifecycle, §26 gate; base fresh origin/main w/ 2-A merged; **APP-LAYER + one governance migration** for the AUDITED resolution): **list page** (`/opportunity`) — bonus periods with a computed opportunity snapshot (RLS reads of bonus_periods + `OpportunityRepository`); **detail page** (`/opportunity/[periodId]`) — **(1) opportunity(X)×performance(Y) quadrant table** (median split into four **"İncele"** cells — advisory, **NEVER a verdict** §4.2/§26; **performance-Y = SUM(point_ledger.points_delta) WHERE event_type='task_approved' — approved POINTS ONLY, NO compensation read ANYWHERE**; half-open period window `[starts_on, ends_on+1d)`; **suppressed small-cohort snapshots excluded** §17), **(2) team opportunity balance** (cohort median + below-median employees with EvidenceDrawer; **manager sees ONLY their own PRIMARY-team employees — no cross-team leakage**, RLS-enforced), **(3) advisory flag review** — the `opportunity_flag` P0 Insights (InsightCard + evidence + actions) each resolved through the **§2.8 status lifecycle** (only the reviewer sub-lifecycle up to accepted\|dismissed is offered — dead applied/observed buttons filtered out); accept/dismiss **requires a reason** and is **AUDITED (§2.7)**. **Migration 0049** (governance, no engine): after-update `log_audit` trigger on `intelligence_insights` (§2.7) + **column-restricted UPDATE grant** (`status, resolved_at, resolution_code` only — a resolver can't overwrite the authoritative `deterministic_payload`) + **RLS UPDATE policy** `org=current_org() AND insight_type='opportunity_flag' AND (has_permission('intelligence.manage') OR (subject_type='employee' AND manages_team(team_of(subject_id))))` — **no new table/column/definer → NO type-drift**. **`resolveOpportunityFlagAction`** (`validatedAction` + Zod; accept/dismiss ⇒ reason required) — `requirePermission('intelligence.read')` floor then delegates to `IntelligenceRepository.transition`; uses the **RLS user client, NOT admin**, so `auth.uid()`=the resolver (a non-null §2.7 audit actor) and the RLS UPDATE policy is the real gate; `transition()` now stamps `resolved_at`+`resolution_code` for **accepted** too. The flag list is intersected with the viewer's VISIBLE snapshot subjects so a manager sees only their own team's flags. Every page gates server-side on `hasPermission('intelligence.read')` **AND** `FeatureFlagResolver.isEnabled('opportunity_intelligence')` (→ /unauthorized); **client-side authz never source of truth (AD1)**. Nav "Fırsat Zekası" (TrendingUp; management; `intelligence.read` + `opportunity_intelligence` flag) + layout resolves the flag. **NO protected characteristic; NO compensation; NO automatic pay adjustment; findings are "Investigate" never a verdict; small-cohort suppression respected in every view; NO cross-team leakage; NO LLM; NO Recharts (table-first); barrel imports only (`@/modules/opportunity-intelligence`, `@/modules/intelligence`, viz via `@/components/intelligence`).** Exception-center routing documented DEFERRED (center not built). Catalog stays 23 | `app/(app)/opportunity/{page,loading}.tsx` + `[periodId]/page.tsx`, `src/app/actions/opportunity/resolve-flag.ts`, `src/lib/validation/schemas/opportunity.ts`, `src/components/features/opportunity/{view.ts,quadrant-table,team-balance,flag-review,resolve-flag-button,period-list}`, `src/modules/intelligence/repository/intelligence-repository.ts` (transition accepted-stamp), `migrations/0049`, `tests/0048` (pgTAP), seed fixtures, `src/components/app-nav.tsx` (+TrendingUp nav) + `app/(app)/layout.tsx`, `security/db-surface.yml` + `security/access-matrix.yml` (intelligence_insights update), `tests/unit/actions/opportunity.test.ts` | ✅ **non-DB gates green** | INTEL-P3-2B; **verify:fast** (lint + tsc + **unit 630** + madge no-cycle + **manifest 26**) + **verify:contracts** OK (49 migrations linted, surface classified). Tests: resolve schema (accept/dismiss require reason; enum step); action happy (enforces intelligence.read + delegates to transition via RLS user client) / deny (requirePermission throws → transition NOT called) / validation (blank reason on accept → transition NOT called); auto-covered by `security-boundary` (client ResolveFlagButton imports no admin/module; the action calls requirePermission). **Adversarial multi-agent review** confirmed **11 findings, all fixed**: (A) column-restricted grant so a resolver can't rewrite deterministic_payload; (B) `transition()` dropped reason/timestamp on the **accepted** outcome → now stamped (§2.7); (C) dead applied/observed buttons → intersected with the schema-resolvable set; (D) flag list wasn't team-scoped (intelligence.read is org-wide) → intersected with VISIBLE snapshot subjects (no cross-team leakage); (F) unused commandId dropped; (E) pgTAP hardened; (G) stale access-matrix comment fixed. pgTAP `0048` authored (after-update audit trigger + **RLS UPDATE gating**: finance/subject/**different-team manager** CANNOT resolve; a5 CANNOT resolve a policy_health insight [insight_type scope]; column-restriction → 42501; manager-of-PRIMARY-team CAN reviewed→dismissed; resolution_code persisted; **audit actor_id = the resolving manager** §2.7; cross-tenant blocked; catalog 23). **DB gates deferred (Docker down locally)**: `db reset` / `supabase test db` run on Linux CI; **0049 adds no new table/column → NO `database.generated.ts` regen needed**. **Concludes Module 2 & Phase P3** |
| Phase P4 8-A1 deterministic metric layer + semantic query service (Module 8-A1) | Intelligence V1 metric-execution layer — first slice of Module 8 (implementation_product_9_modules §10.11 semantic query service, §10.10 permission-aware drill, §10.20 bounded ranges, §2.1/§2.2 metric+dimension contract, §26 gate; base fresh origin/main w/ P3 merged; **APP-LAYER ONLY** — NO migration, NO DB/view/permission change, catalog stays 23, no type-drift). Executes the P0 metric SSOT for **9 of 11** MetricIds via the **RLS user client** (cap_hit_rate + dispute_rate deferred to 8-A2 per the accepted pre-flight split). **Semantic query service** (`application/semantic-query-service.ts`): behind the `intelligence` flag (fail-closed) → **P0 `validateSemanticQuery`** (permission + dimension catalog + sensitive-dim `intelligence.manage`) → **executability gate** (typed REJECT, never a silent subset/0: `metric_not_executable` for the 2 deferred metrics, `dimension_not_executable` for registry-allowed-but-unservable dims / >1 group-by, `filter_not_executable` for non-eq/in ops, `comparison_not_executable` for a non-anchored comparison) → bounded **period resolver** (`bonus_period`/`range`/`relative`; relative resolved against ordered `bonus_periods`, **not the wall clock** — deterministic) → **parameterized** executors (NO raw SQL, NO NL, NO LLM §26) → MetricResults + optional **comparison deltas** (previous_period/trailing_3/6/baseline). **9 executors** (exact locked formulas, `sourceVersion='metrics-v1'`, empty/undefined slices omitted except the payout_total sum): opportunity_index (mean of non-null `opportunity_snapshots.opportunity_index`, suppressed NULL excluded), policy_complexity (latest `total_score` per policy_version; org=mean), payout_total/payout_concentration(HHI)/budget_variance (**via `v_finance_payout` / `v_finance_period_totals` ONLY — SI-12, no raw points/comp**), cycle_completion_rate (approved÷terminal, `completed_at` window), approval_latency (median `approved_at−submitted_at`), manual_override_rate (`manual_adjustment`÷scoring entries, `created_at` window), gaming_flag_rate (distinct confirmed-flag employees ÷ scored population). **Each executor declares a servable dimension subset (⊆ registry allowedDimensions, enforced at registry build)** — the broad P0 registry stays the cross-phase contract; the exec layer narrows per-phase and rejects the rest. **Drill scaffolding** (`application/drill-service.ts`, §10.10): `buildDrillQuery`/`availableDrillLevels` map company→team→employee to a servable group-by (employee = sensitive, gated at execution); drill UI is 8-B/8-C. Public API via the barrel only (`executeSemanticQuery`, `buildDrillQuery`, `availableDrillLevels`, `isMetricExecutable`, `servableDimensions`, `METRICS_RULESET_VERSION`); no deep imports. **NO forecast/anomaly (P8); NO Python (§10.14); NO warehouse (§10.15); NO UI (8-B/8-C); NO raw SQL/LLM (§26); reads via the RLS user client only (never admin).** | `src/modules/intelligence/metrics/executors/{types,errors,period-resolver,precomputed,finance,tasks,activity,registry}.ts` (8), `src/modules/intelligence/application/{semantic-query-service,drill-service}.ts` (2), `src/modules/intelligence/index.ts` (barrel), `src/modules/module-manifest.json` (+publicApi/`application` layer), `tests/unit/modules/intelligence/{semantic-query-service.test,fake-supabase}.ts` | ✅ **gates green** | INTEL-P4-8A1; **verify:fast** (lint + tsc + **unit 652** + madge no-cycle + **manifest 26**) + **verify:contracts** OK (no new migration). Tests (23): exact deterministic formula per metric (worked-example golden with fixed rows via a filtering in-memory Supabase fake — real period-windowing/filtering/grouping); semantic-query rejections (feature-off; validator disallowed-dim; **sensitive-employee-dim denied without intelligence.manage**; `metric_not_executable` cap_hit_rate/dispute_rate; `dimension_not_executable` budget_variance-by-team + >1 group-by; `filter_not_executable`); **SI-12 routing** (payout_total reads `v_finance_payout`, asserts NOT `bonus_allocations`/`point_ledger`); comparison delta (previous_period); bonus_period-filter narrowing; drill levels/build. **Adversarial multi-agent review** (4 lenses + Opus arbiter) → **0 blocking defects**: all 4 "NULL-handling" flags refuted (columns are NOT NULL per 0048/0045/0019; `v_finance_payout` filters employee_id at source) and the 5 "registry⊋servable" flags refuted (intended two-gate design; narrowing the registry would break the cross-phase contract). **Golden coverage = the deterministic vitest suite** (8-A1 adds no DB surface → DoD "Otherwise" clause; no new pgTAP — the underlying tables/views/RLS are already anchored by 0009/0013/0019/0027/0045/0047; shared seed + existing pgTAP UNTOUCHED). **DEFERRED to 8-A2**: cap_hit_rate (needs the `v_finance_cap_stats` view migration) + dispute_rate (needs a disputes period/team/policy_version decision); payout_total role/team/manager + payout_concentration team dims (need a finance-view extension). First slice of Module 8 & Phase P4 |
| Phase P4 8-A2 cap_hit_rate + dispute_rate executors + golden pgTAP (Module 8-A2) | Completes the Intelligence V1 metric layer — **all 11 MetricIds now have executors** (implementation_product_9_modules §10.6 cap [HR/policy audience], §10.11, §10.21, §26; base fresh origin/main w/ 8-A1 merged; **MIGRATION-FREE** — no supabase/migrations, no database.generated.ts change → **no type-drift**; catalog stays 23). **cap_hit_rate** (`executors/cap-dispute.ts`) ← `bonus_allocations`: `count(cap_applied='yes')/count(*)` for the period; servable `{organization, bonus_period, team, role}` (team=`primary_team_id`; role via a `memberships.primary_role` join — HR/Auditor can read the roster). Because `bonus_allocations` RLS is HR/Auditor-only (SI-12 excludes Finance/owner/admin/manager), the executor declares **`availableRoles={hr,auditor}`**; the service's executability gate **REJECTS a source-excluded caller (incl. Finance) with the NEW typed `metric_not_available_for_role`** BEFORE any query (role from `getActiveOrg()`, threaded via `ctx.role`; **fail-closed** when role absent) — an explicit denial, never a silent 0%. Finance cap visibility (an SI-12-safe `v_finance_cap_stats` view) is a **documented DEFERRED** enhancement — no Finance cap view, no migration here. **dispute_rate** ← `disputes`: the P0 registry `allowedDimensions` is **TRIMMED** to `['organization','dispute_type']` (a contract correction — the disputes schema has no bonus_period/team/policy_version, `target_id` is polymorphic FK-less), so the P0 validator now rejects those dims; formula `count(disputes opened in the window [by dispute_type]) / count(DISTINCT task_approved employees in the window) × 100` (scored-population denominator, consistent with gaming_flag_rate; period via a date-window on `opened_at`; omit-on-empty). **End-to-end golden pgTAP** (`supabase/tests/0049_intelligence_metrics_golden.test.sql`): a **self-contained fresh org `d0…`** (no collision with seeded orgs → deterministic; shared seed UNTOUCHED) seeded inside `BEGIN…ROLLBACK` under `session_replication_role='replica'` (bypasses the state-machine/FK triggers to seed terminal states directly; CHECK + RLS still enforced; identity graph auth.users→profiles→memberships still seeded in-order for robustness), then **each executable metric's locked formula is replicated in SQL and asserted to the exact value** (cap_hit_rate 50, cycle 75, approval_latency 4000ms, manual_override 25, gaming_flag 50, opportunity_index 70, dispute_rate 100, payout_total 10,000,000, payout_concentration HHI 0.52, budget_variance 25, policy_complexity 40) via the **real RLS read path** — driven as HR (org-wide), **Finance** (reads `v_finance_*` aggregates but **0 raw bonus_allocations/point_ledger** rows → the SI-12 DB fact cap_hit_rate's reject rests on), **employee** (self-only), and a cross-tenant **Org A actor** (sees 0 golden-org rows). **NO migration/DB surface (no type-drift); NO Finance cap view (deferred); NO raw SQL/NL/LLM (§26); reads via the RLS user client only; NO dashboards/forecast.** | `src/modules/intelligence/metrics/executors/cap-dispute.ts` (new), `executors/{registry,types,errors}.ts` (extend: entry availableRoles + new `metric_not_available_for_role`), `src/modules/intelligence/metrics/registry.ts` (dispute_rate dim trim), `src/modules/intelligence/application/semantic-query-service.ts` (`ctx.role` + role gate), `supabase/tests/0049_intelligence_metrics_golden.test.sql` (new golden pgTAP), `tests/unit/modules/intelligence/semantic-query-service.test.ts` (+8-A2 cases) | ✅ **non-DB gates green** | INTEL-P4-8A2; **verify:fast** (lint + tsc + **unit 659** + madge no-cycle + **manifest 26**) + **verify:contracts** OK (no new migration). Unit tests: cap_hit_rate formula (hr) + **role-based reject** (finance + missing-role → `metric_not_available_for_role`) + role-group via memberships join; dispute_rate formula + by-type + omit-on-empty + the **trimmed-dim rejection** (bonus_period/team/policy_version → `dimension_not_allowed_for_metric`); updated the 8-A1 tests that assumed cap/dispute were non-executable (now all 11 executable, drill levels cap_hit_rate=[company,team]). **Adversarial multi-agent review** (3 lenses + Opus arbiter, weighted on the un-runnable pgTAP) → **0 blocking defects**: executor + all 11 SQL-mirrored assertion values independently recomputed OK; a lens's "missing profiles FK" flag was refuted (replica mode skips FK RI triggers) — but I still added the auth.users/profiles identity graph so the fixture is valid under either FK interpretation. **DB gates deferred (Docker down locally)**: `db reset` / `supabase test db` (0041/0047/0048 + the new **0049**) run on Linux CI; shared seed untouched → existing exact-count tests unaffected. Concludes Module 8-A (V1 metric layer). **Still DEFERRED**: Finance `v_finance_cap_stats` cap view; payout_total role/team/manager + payout_concentration team dims (finance-view extension); dashboards 8-B/8-C; forecast P8 |
| Phase P4 8-B1 intelligence dashboards — viz platform + Executive Overview (Module 8-B1) | Intelligence V1 UI — first slice of Module 8-B; introduces the CHARTING SYSTEM + the Executive Overview dashboard (implementation_product_9_modules §10.2/§10.3/§10.10/§10.13/§10.16/§18/§19/§23/§26; base fresh origin/main w/ 8-A2 merged; **APP-LAYER ONLY** — NO migration/DB/permission change, no type-drift). Pre-flight proposed the coordinator's split (viz+Executive now / Financial 8-B2 — the Financial money-delta cards need new finance-safe views = migrations, out of 8-B's app-layer scope); "continue" → implemented **8-B1**. **Recharts `^3.10.1`** added (React 19-compatible; **`npm audit --audit-level=high` = 0**; **`next build` green**). **Accessible chart primitives** (`src/components/intelligence/charts/`): Sparkline, TrendChart (line), DistributionChart (bar), WaterfallChart (stacked base+magnitude), a shared **ChartDataTable** (the §19 accessible source-of-truth: every chart pairs its aria-hidden SVG with a `<details>` data table), a **ChartFrame** (§10.16 contract: question + metric-definition + filter chips + drill + explain + action — no decorative charts §23), YAxis labels, direction by text/glyph (▲/▼/→) **never color-only**. **MetricCard** extended to the §10.3 contract (+trend sparkline, +drill action slot). **Executive Overview** (`app/(app)/executive/page.tsx`, RSC): role-adaptive cards each from a REAL deterministic source or an honest EmptyState (**never a fabricated number**) — Döngü Sağlığı (`cycle_completion_rate`), Ödeme Projeksiyonu (**deterministic Known+Projected only; statistical band = P8 §10.13**, via payout_total + `v_finance_period_totals`), Bütçe Sapması (`budget_variance`), Kritik İstisnalar (`IntelligenceRepository` insights), Açık İtirazlar (direct RLS `disputes` count), Politika Sağlığı (`IncentiveHealthRepository` latest overall), Mutabakat (`runReconciliation`, finance/auditor-gated else honest role-restricted) — plus **"Ne değişti?"** (comparison deltas) + **"İnceleme gerektirenler"** (non-terminal warning/critical insights) + a **payout trend chart** with a **permission-aware drill** (§10.10). All metric data flows through **`executeSemanticQuery`** (validated whitelisted plans → parameterized queries — **NO raw SQL/NL/LLM §26**); **Finance financial metrics via `v_finance_*` only (SI-12)** — a role scoped out by RLS yields an empty result → honest "unavailable", not a fake 0 (the payout trend returns an empty series when RLS-denied). **Drill server action** (`src/app/actions/intelligence/drill.ts`, `validatedAction` + Zod) — `requirePermission('intelligence.read')` floor, then `buildDrillQuery` + `executeSemanticQuery` (re-validates; sensitive employee level needs `intelligence.manage`; typed rejects). Every page gates server-side on `hasPermission('intelligence.read')` **AND** `FeatureFlagResolver.isEnabled('intelligence')` (→ /unauthorized); client-side authz never SoT (AD1). Nav "Yönetici Bakışı" (BarChart3; `intelligence.read` + `intelligence` flag) + layout resolves the flag. Pure view-model helpers (`features/executive/model.ts`). **NO forecast/anomaly (P8); NO Python (§10.14); NO warehouse (§10.15); NO Operations/Policy dashboard or insight engine (8-C); barrel imports only (`@/modules/intelligence`, viz via `@/components/intelligence`).** | `package.json` (+recharts ^3.10.1), `src/components/intelligence/charts/{sparkline,data-table,trend-chart,distribution-chart,waterfall-chart,chart-frame}.tsx` (6) + `MetricCard.tsx` (+§10.3) + `index.ts`, `src/components/features/executive/{model.ts,drill-panel.tsx}`, `src/app/actions/intelligence/drill.ts` + `src/lib/validation/schemas/intelligence-drill.ts`, `app/(app)/executive/{page,loading}.tsx`, `src/components/app-nav.tsx` (+BarChart3 nav) + `app/(app)/layout.tsx` (resolve `intelligence` flag), `tests/unit/{modules/intelligence/charts.test.tsx,components/executive-model.test.ts,actions/intelligence-drill.test.ts}` | ✅ **gates green** | INTEL-P4-8B1; **verify:fast** (lint + tsc + **unit 676** + madge no-cycle + **manifest 26**) + **verify:contracts** OK (no migration) + **`npm audit --audit-level=high` 0** (after Recharts) + **`next build` green** (`/executive` dynamic). Tests (17 new): chart-primitive **§19 a11y** (table alternative renders via `getByRole('table')`; direction glyph not color; sparkline text label; ChartFrame question+definition); Executive **view-model** from a **mocked executeSemanticQuery outcome** (org-level read ignores grouped rows; changesFrom sorts by magnitude/drops zero; attention excludes terminal+info; formatMetric kuruş→₺); drill **action gating** (allow/deny → plan never built; unservable-level typed reject; bad-metric schema reject); auto-covered by `security-boundary`. **Adversarial multi-agent review** (3 lenses) fixed a **HIGH SI-12 honesty defect** (payout trend rendered misleading zeros for non-finance → now returns an empty series → honest EmptyState) + §19 YAxis labels + drill-table aria-label + a non-finite-value guard + `safe()` error logging; dropped false positives (sparkline `role=img` valid; `PermissionError` pattern pre-existing; `FeatureFlagResolver.isEnabled` already fail-closed). **DEFERRED to 8-B2**: Financial Intelligence dashboard (real `v_finance_period_totals` cards + the cap/adjustment/dispute-money-delta + team-cost cards needing new finance-safe views/migrations). First dashboard of Module 8-B & Release A's Intelligence UI |
| Phase P4 8-B2 Financial Intelligence dashboard (Module 8-B2) | Second Intelligence V1 dashboard (§10.4), **APP-LAYER ONLY** over the EXISTING finance-safe surface + the 8-A metric layer, **reusing the 8-B1 viz platform** (base fresh origin/main w/ 8-B1 merged; **NO migration / NO new DB object / NO new metric id / NO new permission / NO new npm dependency** → no type-drift, catalog stays 23). **`app/(app)/financial/page.tsx`** (RSC, role-adaptive): each §10.4 card is a REAL value or an honest EmptyState — **never a fabricated number** (§23). **(i) Registered metrics** via `executeSemanticQuery`: Toplam Ödeme (`payout_total`), Bütçe Sapması (`budget_variance`), Ödeme Yoğunlaşması (`payout_concentration` HHI), Cap-Hit Oranı (`cap_hit_rate` — **HR/Auditor-only** via the metric's role gate, queried SEPARATELY so its reject doesn't poison the finance bundle). **(ii) Sanctioned finance-view roll-ups** (thin RLS read of the `security_invoker` `v_finance_period_totals` — a granted view, not a base table, not raw SQL): Havuz/Dağıtılabilir/Tahakkuk/Ödenen/Bekleyen(=accrued−paid)/Dağıtılmamış + **Havuz Kullanımı (derived** = accrued/pool·100 — no new metric). Charts (reused 8-B1 primitives, §19 + §10.16 ChartFrame): a **waterfall** that reconciles by construction (Havuz → −Dağıtılmayan → Tahakkuk → −Bekleyen → Ödenen), an **accrual trend** (`total_accrued` over trailing-6 periods) with a **permission-aware drill** on `payout_total` (§10.10, reused DrillPanel/action), and a **concentration distribution** (`v_finance_payout` per-employee amounts binned). **(iii) DEFERRED money-delta enrichment cards** — Cap Para Etkisi / Düzeltme Para Etkisi / İtiraz Finansal Etkisi / Takım Maliyeti / Çalışan Başına Maliyet — rendered as an honest "henüz mevcut değil" (need new finance-safe views = the 8-B3 DB slice; **no fake number**). **SI-12:** all money via `v_finance_*` / the metric layer only — never raw `bonus_allocations`/`point_ledger`/comp; an RLS-denied (non-finance/auditor) read yields an EMPTY result → honest "unavailable", NOT a fake 0. Server-side `hasPermission('intelligence.read')` + `FeatureFlagResolver.isEnabled('intelligence')` gate (→ /unauthorized); client-side authz never SoT (AD1). Nav "Finansal Zeka" (Coins; `intelligence.read` + `intelligence` flag; layout already resolves the flag). Pure view-model (`features/financial/model.ts`, reuses `readOrgMetric`/`formatMetric`). **NO statistical forecast (P8); NO raw/NL/LLM SQL (§26); NO deep cross-module imports; barrel imports only.** | `app/(app)/financial/{page,loading}.tsx`, `src/components/features/financial/model.ts`, `src/components/app-nav.tsx` (+Coins nav), `tests/unit/components/financial-model.test.ts`; **+ SI-12 honesty fix** to `src/modules/intelligence/metrics/executors/finance.ts` (payout_total org-level now OMITS an empty read instead of emitting ₺0) + its 8-A1 test | ✅ **gates green** | INTEL-P4-8B2; **verify:fast** (lint + tsc + **unit 686** + madge no-cycle + **manifest 26**) + **verify:contracts** OK (no migration) + **`npm audit --audit-level=high` 0** (no new dep) + **`next build` green** (`/financial` dynamic). Tests (10 new): financial view-model from mocked finance-view rows + a mocked executeSemanticQuery outcome — deriveRollup (outstanding, derived pool-util null when no pool), the reconciling waterfall (running total = paid), binPayouts (positive-only, empty→[]), the 5 deferred cards, and the **SI-12 honesty** unit (RLS-denied read → null/[] → unavailable, never a fake 0); drill action gating reused from the 8-B1 test. **Adversarial multi-agent review** (3 lenses + Opus arbiter) confirmed **2 HIGH SI-12 fabricated-zero defects, both fixed**: (1) `payoutTotalExecutor` org-level emitted ₺0 on an empty (RLS-denied) read → now returns [] (honest unavailable), aligning it with the metric's own period/employee branches + budget_variance/payout_concentration (8-A1 test updated); (2) `loadAccrualTrend` filled missing periods with a fabricated 0 → now plots only periods with a real finance row. Dropped the MEDIUM (the waterfall delta is a by-construction running total; the snapshot `undistributed` is surfaced verbatim in its own card). a11y + boundary lenses = 0 defects. **DEFERRED (8-B3 finance-metric DB slice):** SI-12-safe views for cap MONEY impact, adjustment money impact, dispute financial impact, per-team payout (team-cost), and a headcount source (cost-per-employee). Completes the Module 8-B dashboards' in-scope §10.4 surface |
| Phase P4 8-B3 finance money-delta views + metrics (Module 8-B3) | The DB + metric-SSOT + golden half of the money-delta cards 8-B2 honestly DEFERRED (implementation_product_9_modules §10.4 Financial Intelligence tier iii, §24 metric author, §26 gate, CLAUDE.md SI-12; base branch `product/p4-8b3-finance-metrics`; **dashboard wiring = 8-B4**, a separate later slice — `app/(app)/financial` UNTOUCHED here). **Source-availability gate**: cap-money-impact / per-team payout / cost-per-employee are REAL money aggregates the granted `security_invoker` finance views can't produce because their sources' RLS **excludes Finance** (`bonus_allocations` 0013 HR/Auditor-only; `team_memberships` + `memberships` 0007 Finance-excluded) → they need **definer-rights views**; dispute-financial-impact + adjustment-money-impact stay DEFERRED (no SI-12-safe money source — `bonus_ledger` has no dispute_id, point_ledger adjustments are POINTS not money). **Migration 0050 — THREE definer-rights VIEWS** (`with (security_invoker=true)` DELIBERATELY OMITTED so each runs as OWNER and bypasses base-table FORCE RLS; `has_role()`/`current_org()` still evaluate the CALLER's JWT sub), each **SELF-GATED** `WHERE organization_id = public.current_org() AND (public.has_role('hr') OR public.has_role('finance') OR public.has_role('auditor'))`, every reference fully-qualified `public.*` (no search_path dependence): **v_finance_cap_impact** = `Σ(raw_share_minor − final_amount_minor) FILTER (cap_applied='yes')` per (period, primary team AD9) [≥0 by INV-4]; **v_finance_team_cost** = per SNAPSHOT-team net accrual using the **byte-identical** net-accrual formula as 0027 `v_finance_payout`/`v_finance_period_totals` (`Σ amount_minor FILTER bonus_accrual/accrual/credit − Σ FILTER reversal/accrual/debit`, via `bonus_ledger → bonus_allocation_snapshots → bonus_allocations` on the run's unique (calculation_run_id, employee_id), team = `bonus_allocations.primary_team_id` — the FROZEN AD9 snapshot team, SAME attribution as cap_impact, so a post-calc live team change does NOT drift a closed period's breakdown; LEFT join → a missing allocation/NULL team counts under team_id=NULL, Σ preserved; team_memberships is NOT read`) so **Σ team_cost reconciles to payout_total**; **v_finance_cost_per_employee** = `floor(net accrual / active headcount)` per period (numerator identical to `total_accrued`; headcount = `memberships status='active'` COUNT scalar). **SI-12 column discipline**: each view projects ONLY {organization_id, bonus_period_id, (team_id where applicable), one minor-currency aggregate} (+ an active_headcount scalar for cost_per_employee) — NEVER raw_share/final/cap_minor/cap_basis/adjusted_score/points/comp or any per-employee row. Grants: `revoke all … from public, anon` + `grant select … to authenticated, service_role` (row visibility gated by the view's own WHERE). **3 metrics** (`metric-id.ts`/`registry.ts` → METRIC_IDS 11→14): `cap_money_impact`, `team_cost` (allowedDimensions organization/team/bonus_period), `cost_per_employee` (organization/bonus_period) — all `unit=minor_currency`, `requiredPermission=intelligence.read`; **executors** (`executors/money-delta.ts`, new) read ONE view each via the RLS user client (an untyped-client cast `as unknown as SupabaseClient`, LOCAL + commented, until `database.generated.ts` is regenerated), an **empty read → `[]`** (honest unavailable, never a fabricated ₺0 §23); `executors/registry.ts` adds `FINANCE_MONEY_ROLES={hr,finance,auditor}` as the `availableRoles` gate so a **source-excluded role → `metric_not_available_for_role`**, never a silent 0. **Catalog STAYS 23** (no new permission). **Golden pgTAP 0049 extended** (same fresh org `d0…`, `session_replication_role='replica'`): capped allocation set to raw 7,000,000 → final 6,000,000 (cap impact 1M); **Finance (authorized) value asserts** — cap_money_impact 1,000,000 / team_cost[f1] 10,000,000 / cost_per_employee 1,666,666 / active_headcount 6 (all hand-reconciled to the fixtures); **BLOCKING negatives BOTH directions** — a non-authorized manager (d4) reads **0 rows** from each view (no over-expose), a cross-tenant Org-A HR (a3) reads **0 golden-org rows** from each view (org isolation independent of the role gate). **NO raw SQL/NL/LLM (§26); NO forecast (P8); definer objects self-enforce authz + tenant isolation; imports via `@/modules/intelligence` only; no edits to `app/(app)/financial` (8-B4).** | `migrations/0050`, `src/modules/intelligence/metrics/executors/money-delta.ts` (new) + `executors/registry.ts` (+FINANCE_MONEY_ROLES + 3 entries), `metrics/{registry,metric-id}.ts` (+3 metrics), `supabase/tests/0049_intelligence_metrics_golden.test.sql` (+value/negative asserts), `security/db-surface.yml` + `tests/unit/security/db-surface-manifest.test.ts` (views 2→5), `tests/unit/modules/intelligence/{semantic-query-service,registry-dimensions}.test.ts` | ✅ **non-DB gates green** | INTEL-P4-8B3; **verify:fast** (lint + tsc + **unit 696** + madge no-cycle + **manifest 26**) + **verify:contracts** OK (50 migrations linted, **5 views** classified) + **`npm audit --audit-level=high` 0** (no new dep). Unit (11 new): 3 executor exact values (finance/hr/auditor) + **org-level cumulative Σ** (multi-period, Σ per-period floors) + **empty→[]** (honest unavailable) + **source-excluded-role reject** (manager/missing → `metric_not_available_for_role`) + non-allowed-dim reject + **routing spy** (cap_money_impact reads ONLY `v_finance_cap_impact`, never `bonus_allocations`/`team_memberships`/`point_ledger`); registry auto-tracks METRIC_IDS=14. **Adversarial multi-agent review** (5 lenses + adversarial verify, 9 agents) → 4 raised, **2 confirmed & fixed**: (MAJOR) `cost_per_employee` bare `::bigint` on `numeric/bigint` **ROUNDS** (→1,666,667) not floors → would FAIL the golden → **wrapped in `floor()`** (repo money convention 0021/0022) so the view returns 1,666,666; (nit) org-level Σ-of-floors reconciliation comment softened (Σₚfloor(aₚ/h) ≠ floor(Σaₚ/h) by <headcount minor units) + a multi-period unit test added. 2 refuted: the "definer-view needs a BYPASSRLS owner" premise (owner is the migration superuser) and a "vacuous cross-tenant" claim (the org gate is load-bearing). **DB gates deferred (Docker down locally)**: `db reset` / `supabase test db` (0041/0047/0048 + **0049 extended**) + regen `database.generated.ts` for **0050** (CI artifact `database-generated-types`) run on Linux CI. First half of Module 8-B's money-delta enrichment; dashboard wiring = 8-B4 |
| Phase P4 8-B4 finance money-delta card wiring (Module 8-B4) — **CLOSES §10.4 + Release A** | Wires the three now-live 8-B3 money-delta metrics into the Financial Intelligence dashboard, replacing their honest placeholders (implementation_product_9_modules §10.3/§10.4/§10.10/§10.16/§19/§23/§26/§28 Release A; base fresh origin/main w/ 8-B3 merged [PR #54, 0050 + the 3 views + regenerated types on main]; **APP-LAYER ONLY** — NO migration/new DB object/new metric/new permission/new dependency → **no type-drift, catalog stays 23**). **`app/(app)/financial/page.tsx`**: the three §10.4-iii DEFERRED cards are now **LIVE** via `executeSemanticQuery` (§10.3 MetricCard) — **Cap Para Etkisi** (`cap_money_impact`), **Takım Maliyeti** (`team_cost`), **Çalışan Başına Maliyet** (`cost_per_employee`) — queried in an **ISOLATED `{hr,finance,auditor}`-gated `safe()` bundle** (separate `executeSemanticQuery`, mirroring the `cap_hit_rate` isolation) so a non-authorized role's reject (`metric_not_available_for_role`) → `null` → honest **UnavailableCard**, **NEVER a fabricated ₺0** (SI-12/§23); each read via `readOrgMetric` → `MoneyCard` (minor_currency). **`DEFERRED_MONEY_CARDS` trimmed 5→2** — only **Düzeltme Para Etkisi** + **İtiraz Finansal Etkisi** remain (**no SI-12-safe money source**: `bonus_ledger` has no `dispute_id`; `point_ledger` adjustments are POINTS not money); the on-page + model copy corrected so it no longer implies all five land in 8-B3. **Enrichment (§10.10)**: ONE **permission-aware per-team `team_cost` drill** reusing the existing `DrillPanel` + `drillMetricAction` + `availableDrillLevels('team_cost')`=`[company,team]` (**zero new schema/action**; the action re-validates the `{hr,finance,auditor}` gate + RLS **server-side** — client-side authz never SoT, AD1; team = the frozen **AD9 snapshot** team), rendered ONLY when the org-level `team_cost` is itself readable (else nothing to drill). **Removed the now-DEAD `as unknown as SupabaseClient` cast** in `money-delta.ts` → typed client + **literal selects** + a shared pure `aggregatePeriodTeamMoney` (the 0050 views are now in `database.generated.ts`; the dynamic-template select that forced the cast is gone; **tsc green, no type-drift**). **SI-12**: all money via `executeSemanticQuery`/the sanctioned finance surface only — never raw `bonus_allocations`/`point_ledger`/comp; role/RLS-denied → honest unavailable, never a fake 0. **NO raw SQL/NL/LLM (§26); NO forecast (P8); no un-defer/fabrication of the adjustment/dispute money cards; barrel imports only.** | `app/(app)/financial/page.tsx` (+isolated money bundle + 3 MoneyCards + team_cost DrillPanel + copy), `src/components/features/financial/model.ts` (DEFERRED_MONEY_CARDS 5→2 + doc), `src/modules/intelligence/metrics/executors/money-delta.ts` (dead-cast removal → typed client + literal selects + shared aggregator), `tests/unit/components/financial-model.test.ts` (2 deferred cards + 3 money-metric wiring cases) | ✅ **gates green** | INTEL-P4-8B4; **verify:fast** (lint + tsc + **unit 698** + madge no-cycle + **manifest 26**) + **verify:contracts** OK (no migration; 50 migrations linted, 5 views classified) + **`npm audit --audit-level=high` 0** (no new dep) + **`next build` green** (`/financial` dynamic). Tests (+2 new): the 3 money-delta metrics read as org-level minor_currency values + kuruş→₺; **SI-12 role-denied isolated bundle → null for EVERY money card (→ UnavailableCard, never ₺0)**; `DEFERRED_MONEY_CARDS` now exactly the 2 remaining; existing suites unchanged. **Adversarial multi-agent review** (3 lenses [SI-12/role-isolation, dead-cast regression, governance/copy] + adversarial verify, 5 agents) → **2 raised, 0 confirmed (both refuted)**. **No DB gate (no migration).** **Closes Module 8-B, §10.4 in-scope, and Release A's Intelligence dashboards.** |
| Phase P4 8-C1 Operations Intelligence dashboard (Module 8-C1) | The Operations Intelligence dashboard (§10.8) — first of the 3 slices finishing Module 8 (8-C1 ops → 8-C2 policy §10.9 → 8-C3 deterministic insight engine §10.12; the latter two NOT built here) (implementation_product_9_modules §10.1/§10.3/§10.8/§10.10/§10.16/§18/§19/§23/§26; base fresh origin/main w/ 8-B4 merged; **APP-LAYER ONLY** over the 8-A metric layer + insight store, reusing the 8-B viz platform; **NO migration/new DB object/metric/permission/dependency → no type-drift, catalog stays 23**). **`app/(app)/operations/page.tsx`** (RSC, role-adaptive) — six cards, each a REAL value or an honest **UnavailableCard** (SI-12, never a fake 0): **Döngü Sağlığı** (`cycle_completion_rate`), **Onay Gecikmesi** (`approval_latency`, duration_ms→sn), **Manuel Müdahale** (`manual_override_rate`), **Oyunlaştırma Bayrağı** (`gaming_flag_rate`, aggregate rate — not per-employee), **İtiraz Oranı** (`dispute_rate`) — all via `executeSemanticQuery` (none role-gated → one `safe()` bundle) — plus **İstisna Hacmi** (open warning/critical insight COUNT via `IntelligenceRepository.list`, RLS-scoped; renders unavailable — NOT a fabricated 0 — when the read genuinely fails). **Charts** (§10.16 ChartFrame + §19 accessible via the 8-B primitives — aria-hidden SVG + data-table alt, glyph-not-color): an **approval-latency trend** (per-period reads — `approval_latency` is not servable by `bonus_period`, so each period is queried on its own window, omitting empty periods rather than plotting a fabricated 0) + a **permission-aware drill** (`approval_latency` org→team, **NEVER employee** — aggregate-only, no surveillance; `drillMetricAction` re-validates server-side); an **open-exception breakdown distribution** by insight type (`insightBreakdown`, existing insight-store source). **"Ne değişti?"** (comparison deltas via `changesFrom`) + **"İnceleme gerektirenler"** (`attentionInsights`), mirroring the executive pattern. **DEFERRED honestly (§23, no fabricated data):** İnceleme Gecikmesi (review latency) / Hatırlatma Sayısı / Geciken (overdue) / Yeniden Hesaplama / **Yönetim Eforu Tasarrufu** (Admin Effort Saved — §10.8: **no false hours-saved claim** without a defined methodology; no automated-action source in scope → deferred, not invented). Nav **"Operasyon Zekası"** (Activity; `intelligence.read` + `intelligence` flag). Server-side `hasPermission('intelligence.read')` + `FeatureFlagResolver.isEnabled('intelligence')` gate (→ /unauthorized); client-side authz never SoT (AD1). **AGGREGATE process signals only — NO per-employee monitoring/surveillance** (CLAUDE.md; drills stop at team). **NO raw SQL/NL/LLM (§26); NO forecast (P8); barrel imports only.** | `app/(app)/operations/{page,loading}.tsx`, `src/components/features/operations/model.ts` (insightBreakdown + DEFERRED_OPS_CARDS + reader re-exports), `src/components/app-nav.tsx` (+Activity "Operasyon Zekası"), `tests/unit/components/operations-model.test.ts` | ✅ **gates green** | INTEL-P4-8C1; **verify:fast** (lint + tsc + **unit 712** + madge no-cycle + **manifest 26**) + **verify:contracts** OK (no migration) + **`npm audit --audit-level=high` 0** (no new dep) + **`next build` green** (`/operations` dynamic). Tests (14): metric reads + SI-12-honest role/RLS-denied → null (never a fake 0); `insightBreakdown` groups non-terminal warning/critical by type; deferred-list = the 5 honest cards incl. no-hours-saved Admin-Effort; `changesFrom` ordering; a page-guard (server-side gating + deny path; executeSemanticQuery-only, no admin client; **no employee-level drill**; comparison anchored to a bonus_period; latency ms→sn on both paths; İstisna Hacmi honest-unavailable). **Adversarial multi-agent review** (3 lenses + adversarial verify, 7 agents) → **4 confirmed & FIXED**: (BLOCKER) previous-period comparison is only executable on a `bonus_period` selector — the service fails the WHOLE query on a relative selector (`comparison_not_executable`) → **anchored the bundle to the current bonus_period, comparison applied only when anchored** (without it every card would falsely render unavailable); (MAJOR) `approval_latency` delta is raw ms mislabeled 'sn' in "Ne değişti?" → **÷1000 to match the card** (no 1000× overstate); (minor×2) İstisna Hacmi fabricated 0 on a failed insight read → UnavailableCard; latency-delta consistency. **NOTE (out of 8-C1 scope, flagged for follow-up):** the shipped **executive + financial** pages carry the SAME relative+comparison latent defect (their main metric bundles pass `comparison` with a relative selector → whole-query reject → cards render unavailable) — not touched here (no unrelated refactor). **No DB gate (no migration).** First slice finishing Module 8; Policy dashboard = 8-C2, insight engine = 8-C3. |
| Phase P4 FIX — anchor executive + financial metric bundles (comparison_not_executable) | **Bug fix** repairing a shipped defect in the 8-B1 **executive** + 8-B2 **financial** dashboards (surfaced by the 8-C1 review): both built their PRIMARY metric bundle with `period {kind:'relative', trailing:'current'}` + `comparison {basis:'previous_period'}`. `resolveComparisonPeriod` (period-resolver.ts:104) rejects a non-anchored selector with `comparison_not_executable`, and the semantic service (semantic-query-service.ts:~180) turns that into a **whole-query `ok:false`** — so every primary metric card rendered as an UnavailableCard and "Ne değişti?" was always empty against a real DB. The mocked-OUTCOME view-model unit tests missed it (they never exercised the real service comparison path). **APP-LAYER ONLY; NO migration/new DB object/metric/permission/dependency (catalog 23); NO change to the semantic-query service contract** (page-level fix). **Fix:** ONE pure shared helper **`anchoredMetricPeriod(currentPeriodId)`** in `executive/model.ts` (re-exported via `financial/model.ts` + `operations/model.ts`) → a current period → `{ period:{kind:'bonus_period', bonusPeriodId}, comparison:{basis:'previous_period'} }`; no period → `{ period:{kind:'relative', trailing:'current'} }` **WITHOUT** comparison (cards still render org-level; no delta). Wired into **executive** (new `currentPeriodId` resolve) + **financial** (existing `periodId`) primary bundles; **operations** refactored onto the same helper (behavior-preserving — no three divergent copies). SI-12 unchanged (finance via `v_finance_*`, honest empty on RLS-deny, never a fake 0); the `cap_hit_rate` + money-delta bundles (no comparison) and the trailing-6 trend queries are untouched. **NO raw SQL/NL/LLM (§26).** | `src/components/features/executive/model.ts` (+`anchoredMetricPeriod`), `src/components/features/financial/model.ts` + `src/components/features/operations/model.ts` (re-export), `app/(app)/executive/page.tsx` (+`currentPeriodId` + helper), `app/(app)/financial/page.tsx` (helper), `app/(app)/operations/page.tsx` (refactor to helper), `tests/unit/modules/intelligence/semantic-query-service.test.ts` (+regression), `tests/unit/components/{executive,financial,operations}-model.test.ts` | ✅ **gates green** | INTEL-P4-FIX; **verify:fast** (lint + tsc + **unit 718** + madge no-cycle + **manifest 26**) + **verify:contracts** OK (no migration) + **`npm audit --audit-level=high` 0** + **`next build` green** (`/executive`, `/financial`, `/operations` dynamic). **REGRESSION test (the gap that hid the bug — exercises the REAL service via `fakeSupabase`, NOT a mocked outcome):** a relative selector + comparison → **`ok:false` `comparison_not_executable`** (fails on the OLD construction); an anchored `bonus_period` selector + comparison → **`ok:true` with a previous-period delta** (passes on the FIX). Plus a pure `anchoredMetricPeriod` unit test (present → anchored+comparison; absent → relative + no comparison) + executive/financial/operations **page-guards** (use the helper, never inline relative+comparison). **No adversarial review** (small, well-scoped page-level fix with direct service-level regression proof). **FLAGGED as a follow-up (NOT bundled):** a more robust service-level graceful degradation — when comparison is not executable, resolve the metrics and OMIT the delta instead of failing the whole query — which changes the core-service contract + its existing rejection tests. **No DB gate (no migration).** |
| Phase P4 8-C2 Policy Intelligence dashboard (Module 8-C2) | The Policy Intelligence dashboard (§10.9) — 2nd of the 3 slices finishing Module 8 (8-C1 ops ✅ → 8-C2 policy → 8-C3 insight engine §10.12, NOT built here; NO LLM/"Policy Doctor" = P9) (implementation_product_9_modules §10.1/§10.3/§10.9/§10.16/§18/§19/§23/§26; base fresh origin/main w/ the comparison-anchor fix merged; **APP-LAYER ONLY**, **REUSES the existing policy engines' public APIs** — no re-implementation; **NO migration/new DB object/metric/permission/feature flag/dependency → no type-drift, catalog stays 23**). **`app/(app)/policy-intelligence/page.tsx`** (RSC): a deterministic **version-vs-version comparison** across the §10.9 dimensions via **`getHealthComparison`** (Sağlık Motoru `DimensionDelta` — `dispute_exposure` / `gaming_resistance` / `manager_discretion`(override) / `complexity` / `payout_concentration` + `financial_integrity`; scores 0–100, **higher = healthier → positive Δ = improvement** for every dimension), rendered as a §19-accessible **table** (Boyut / Önceki / Güncel / Δ) with **`DeltaBadge`** (glyph, not color-only); plus a **complexity-across-versions trend** via **`getPolicyComplexityTrend`** (`TrendChart`). Each in a §10.16 `ChartFrame` (soru / metrik-tanımı / filtre). **Version selection**: anchor = the freshest `health-v1`-evaluated policy (mirrors the /policy-health detail anchor); **current(anchor) vs previous** (`pickPreviousVersion`, internal to `getHealthComparison`); `hasPrevious=false` → honest **"need ≥2 değerlendirilmiş sürüm"** (null deltas, never fabricated); no evaluated version → honest "not yet evaluated" + cross-link. The **semantic metric layer is NOT used** (it can't slice dispute/gaming/concentration/override by `policy_version`) → single-sourced from the health engine (no inconsistent double-source). **Structural diff (`computePolicyDiff`) DEFERRED** to the cross-linked `/policy-impact` (optional deliverable; avoids a 3rd feature-flag dependency + duplication §23). **Cross-links** to `/policy-health` (risk drivers + accept-risk + derivation), `/policy-debt` (complexity/simplification detail), `/policy-impact` (structural diff + backtest) — **aggregate + link, NOT re-implement** (§23). **Authz**: the page gates server-side on `hasPermission('intelligence.read')` + `FeatureFlagResolver.isEnabled('intelligence')` (nav consistency); every engine call is wrapped in `safe()` so an RLS (policy.manage) denial or a sub-flag (`health_engine`/`policy_debt`) being off → honest **UnavailableCard/EmptyState**, **never a fabricated number** (SI/honest-empty); client-side authz never SoT (AD1). **Aggregate policy signals only** (no per-employee surveillance). Nav **"Politika Zekası"** (GitCompare; `intelligence.read` + `intelligence` flag). **NO raw SQL/NL/LLM (§26); NO forecast (P8); barrel imports only** (`@/modules/incentive-health` / `@/modules/policy-complexity` / `@/modules/intelligence`, viz via `@/components/intelligence`). | `app/(app)/policy-intelligence/{page,loading}.tsx`, `src/components/features/policy-intelligence/model.ts` (comparisonRows + complexityTrendPoints + formatScore + DIMENSION_LABELS/ORDER), `src/components/app-nav.tsx` (+GitCompare "Politika Zekası"), `tests/unit/components/policy-intelligence-model.test.ts` | ✅ **gates green** | INTEL-P4-8C2; **verify:fast** (lint + tsc + **unit 727** + madge no-cycle + **manifest 26**) + **verify:contracts** OK (no migration) + **`npm audit --audit-level=high` 0** (no new dep) + **`next build` green** (`/policy-intelligence` dynamic). Tests (9): `comparisonRows` §10.9 ordering + labels + exact deltas; **need-≥2-versions** honest path (previous/delta stay null — never a fabricated 0); `complexityTrendPoints` ascending map + empty→[]; `formatScore` null→em-dash; a page-guard (server-side gating + deny path; engine-barrel reuse, no admin client; cross-links to /policy-health + /policy-debt + /policy-impact). **Adversarial multi-agent review** (3 lenses [SI-12/authz, correctness/direction/mapping, scope/§23-duplication/governance] + adversarial verify, 3 agents) → **0 raised, 0 confirmed**. **No DB gate (no migration).** Second slice finishing Module 8; insight engine = **8-C3** (concludes Module 8). |
| Phase P4 8-C3 deterministic insight engine (Module 8-C3) — **CLOSES Module 8** | The DETERMINISTIC insight engine (§10.12) — the LAST slice of Module 8 (8-C1 ops ✅ → 8-C2 policy ✅ → 8-C3 ✅). A versioned rule catalog + a server-only engine that reads the semantic metric layer, applies threshold/delta rules, and emits insights (facts + evidence + action) into the shared store idempotently (implementation_product_9_modules §10.12/§2.3/§2.4/§2.8/§12.8/§10.13/§23/§24/§26; base fresh origin/main w/ the comparison-anchor fix merged; **APP/domain-layer ONLY** — dedup option (i); **NO migration/new DB object/metric/permission/feature flag/dependency → no type-drift, catalog stays 23 / 14 metrics**). **Rule catalog** (`domain/insight-rules.ts`, PURE + VERSIONED, `INSIGHT_RULE_SET_VERSION='insight-v1'`): six deterministic rules with explicit thresholds + rationale — `budget_risk` (budget_variance ≥10 warn/≥20 crit), `cycle_slowdown` (cycle_completion_rate Δ ≤−15/≤−30), `dispute_concentration` (dispute_rate ≥20/≥40), `concentration_change` (payout_concentration HHI ≥0.5/≥0.7), `significant_delta` (manual_override_rate Δ ≥+15/≥+30), `threshold_breach` (gaming_flag_rate ≥15/≥30). **NO statistical anomaly/forecast (P8); NO LLM (§26)** — deterministic threshold/delta ONLY. **Engine** (`application/insight-engine.ts`, server-only): resolves the current bonus_period, reads all 6 metrics via **`executeSemanticQuery`** (anchored `bonus_period` + `previous_period` comparison — **NO raw SQL §26**; the injected **service_role** admin client does ORG-WIDE detection, executors still `.eq(organization_id)` → tenant-safe; the metric read is injectable for tests, boundary stays `executeSemanticQuery`), runs the pure catalog → candidates, emits each IDEMPOTENTLY; honest `no_period`/`metrics_unavailable` (the `intelligence` flag off → emit nothing, never fabricate). **IDEMPOTENCY** (new repo method **`recordRecurring`**, server-only): finds a **NON-TERMINAL** insight of identity `{organization_id, insight_type, subject_type, subject_id, bonus_period_id}` (null-safe `.is()`) → **TOUCHES** it (`last_detected_at` + refresh severity/payload/evidence, keep status) else **INSERTS** `'calculated'` — a re-run creates **NO duplicate** (the property a naive `insert()` violates); a `dismissed`/`retrospective` (terminal) match is NOT resurrected → a fresh insight. Every emitted insight satisfies **InsightSchema** (≥1 evidence [the source metric] + ≥1 action route §12.8; TR headline; deterministic facts stamped with the rule-set version); **`model_payload` stays null**. Emitted **org-level** insights (`subject_type='organization'`) are **RLS-gated on READ** (intelligence.read); aggregate signals only (no per-employee; only percent/score facts — no raw money, SI-12). **On-demand trigger** `runInsightEngineAction` (`intelligence.manage`, server-side; injects `createAdminClient` — mirrors `evaluatePolicyHealthAction`; the action never calls `admin.from(`). Public API via the barrel (`runInsightEngine` + `INSIGHT_RULE_SET_VERSION` + `runRules` + types). No UI button (optional — no new dashboard). | `src/modules/intelligence/domain/insight-rules.ts` (new), `application/insight-engine.ts` (new), `repository/intelligence-repository.ts` (+`recordRecurring`), `index.ts` (barrel), `src/app/actions/intelligence/run-insight-engine.ts` + `src/lib/validation/schemas/insight-engine.ts` (new trigger + schema), `tests/unit/modules/intelligence/{insight-rules,insight-engine}.test.ts` + `tests/unit/actions/run-insight-engine.test.ts` | ✅ **gates green** | INTEL-P4-8C3; **verify:fast** (lint + tsc + **unit 747** + madge no-cycle + **manifest 26**) + **verify:contracts** OK (no migration) + **`npm audit --audit-level=high` 0** (no new dep) + **`next build` green** (no new route — the engine is a server action). Tests (20): the pure rule catalog (each rule fires/does-not-fire at its exact threshold + severity + facts; every fired candidate passes `parseInsight` — ≥1 evidence + ≥1 action; `RULE_METRICS` = the 6 metrics); **engine idempotency** via a stateful in-memory fake — `recordRecurring` insert→touch (a 2nd call over the same identity makes NO 2nd row + touches `last_detected_at`; a `dismissed` match → a fresh insert), and `runInsightEngine` **run-twice → detected 2 / inserted 2 then recurred 2, store still 2 rows** (the property a naive insert violates) + `no_period`/`metrics_unavailable` emit nothing; the trigger action authz (`intelligence.manage` allow + deny → engine NOT run; bad-uuid validation). **Adversarial multi-agent review** (3 lenses [determinism/§26-§23/SI-12, dedup/idempotency, authz/tenant-scope] + adversarial verify, 5 agents) → **2 raised, 0 confirmed**. **No DB gate (no migration).** **CLOSES Module 8** (Intelligence dashboards + deterministic insight engine complete). |

**Runtime verification (2026-08-10, local dev stack, npx Supabase CLI 2.109.1):** `supabase db reset`
migrations **0001..0030** + seed temiz uyguladı; `supabase test db` → **Files=24, Tests=831, Result=PASS,
Failed=0** (`0001`..`0024` ok). `db reset`'teki geçici container flake'leri (`ENOTFOUND`/timeout/"exit 1" —
vector/analytics/storage unhealthy) `supabase stop/start` (gerekirse aux servisleri `-x` ile hariç bırakıp
yalnız Postgres) + retry ile temiz geçti; kod/şema sorunu değil.

**compensation_records güvenlik özelliği (AD3/D7/SI-5):** doğrudan **raw SELECT kapalı** (SELECT policy yok;
maaş kolonları selectable değil); ham okuma **yalnız** `read_compensation_record(employee, reason)` ile
(comp.read/auditor + boş olmayan reason); **write + access audit maskeli** → ham maaş/cap `audit_logs`'a
asla düşmez.

**bonus_periods + bonus_pools invariant'ları (AD10/SI-4):** period **pool locked olmadan lock edilemez**
(AD10); locked pool **t_org + locked_at + locked_by** ister; locked pool amount/t_org **immutable** (new
version); locked/non-open period identity (dates/type/org) **immutable** (SI-4); RLS ENABLE+FORCE +
**görev ayrımı** (period.manage=owner/hr vs pool.create=finance); DELETE bloklu.

**bonus_pool_components + bonus_pool_eligibility invariant'ları (D1/D10/AD9/AD10/SI-4):** component MVP
**Safe Pro-Rata** = yalnız `individual` + weight `1.0` (D1); component/eligibility → `bonus_pools` **same-org
composite FK**; eligibility employee **aynı org üyesi olmak zorunda** (`(org, employee)` → `memberships`
composite FK → cross-tenant employee **yapısal olarak imkânsız**); `primary_team_id` same-org (teams composite
FK) + **AD9** `team_memberships.is_primary` trigger doğrulaması; 15-gün eligibility + proration cap üzerinde
(D10); eligibility yazımı **server-only** (employee-own + HR/Finance/Auditor read); component yazımı
`pool.create`; parent pool **draft'tan çıkınca** hem component hem eligibility satırları **immutable** (SI-4);
DELETE bloklu.

**bonus_calculation_runs + bonus_allocations + bonus_allocation_snapshots invariant'ları
(D1/D6/AD6/AD7/AD9/AD10/SI-4/SI-12/SI-13/SI-14):** run **yalnız locked period + locked pool** üzerinde başlar
(AD10 çift guard — referans verilen pool draft/superseded ise blok); state machine `running→completed→superseded`;
idempotency `unique(organization_id, idempotency_key)`; run **completed olunca allocation'lar blanket freeze**
(amount/status/cap/team/policy/run bağları dahil — SI-4/SI-14); snapshot **ince + append-only immutable**
(UPDATE/DELETE hard-block; per-employee detay kopyalanmaz); allocation **cap-not-exceeded** CHECK +
`cap_applied` enum (`pending_missing_cap_basis` dahil — AD6); **approved/exported/paid** yazımı bu dilimde
bloklu; tüm same-org composite FK'ler (period/pool/policy_version/run/employee/team) → cross-tenant yapısal
kapalı (SI-7); yazımlar **server-only** (employee-own allocation read; Finance raw hariç = view-only SI-12);
`Σ(final)+undistributed_remainder = pool` **hard trigger değil**, yalnız seed/test-verified (SI-13/INV-4).

**bonus_ledger invariant'ları (ADR-017/BL-1..4/SI-3/SI-12):** double-entry para defteri (`entry_type`
debit/credit, `account` pool/accrual/payout/clawback); **append-only** (UPDATE/DELETE hard-block; düzeltme =
reversal — BL-1); **deferred (DEFERRABLE INITIALLY DEFERRED) balance trigger** → `Σdebit = Σcredit` per
**(organization_id, transaction_id)** hard-enforce; accrual **yalnız snapshot_id ile** (yapısal — SI-3;
approved-gate Phase 6'ya ertelendi); idempotent accrual `unique(snapshot_id, employee_id, account)`; bu
dilimde **yalnız `bonus_accrual` + `reversal`** yazılabilir (payout/clawback/approval event'leri guard'lı);
raw SELECT **yalnız Finance + Auditor** (HR/Employee/Manager/**Support** hariç — SI-12); yazımlar
**server-only**; same-org composite FK'ler (pool/run/snapshot/employee); INSERT audit (BL-4). **BL-2**
`DEFERRABLE INITIALLY DEFERRED` trigger (0022 `enforce_bonus_ledger_balance`) Σaccrual≤pool_ref AD8-aware hard-enforce eder. **BL-3** `enforce_bonus_ledger_payout_cap()` DEFERRABLE trigger (payout ≤ net accrual, reversal-aware) **Phase 6-c (`0027`)'te eklendi**; `payout_marked_paid` artık yazılabilir (`export_id NOT NULL` kısıtı ile); `payout_exported` / `clawback_*` hâlâ guard'lı. **Clawback** (D2 gated) ve app/UI/API hâlâ hariç.

**disputes + dispute_events invariant'ları (D9/SI-6/SI-7 + ADR-006):** `disputes` **mutable state machine**
(`open→under_review→needs_info→under_review→resolved→closed`) — yasak geçişler + open sonrası kimlik
immutability (org/complainant/type/target/opened_at); `dispute_events` **append-only** ve **SECURITY DEFINER
trigger ile otomatik** yazılır (her INSERT/status-transition'da; `actor_id=auth.uid()`), authenticated
doğrudan yazamaz (UPDATE/DELETE hard-block). **D9:** stored `decision_owner_id` + `owns_review_decision()` +
CHECK'ler (`reviewer ≠ owner`, `reviewer ≠ complainant`) + resolve RLS `NOT owns_review_decision`. **HR-only
assign `has_role('hr')` ile** (yeni `dispute.assign` permission **eklenmedi** — test 0001 permission-katalog
sayımını korumak için, 0001..0008 değiştirilemez). `due_at` **stored** + `due_at > opened_at` sanity CHECK
(**iş-günü hesabı yok** — OQ-DP-1 ertelendi). RLS: complainant + assigned reviewer + HR + Auditor; **Finance/
Support hariç**. `target_id` polymorphic (FK yok). **Hariç:** point_ledger `dispute_adjustment`, recalculation
/ yeni calc run, bonus_ledger reversal/accrual, notification, reopen orkestrasyonu.

**anti_gaming_flags invariant'ları (D5/SI-6/SI-7 + ADR-006):** `anti_gaming_flags` **mutable review lifecycle**
(`open→reviewing→confirmed|dismissed`) — yasak geçişler (skip / `reviewing→open` / terminal) + oluşturma
sonrası kimlik immutability (org/rule/subject/related/evidence/created_at); review-consistency CHECK
(`confirmed|dismissed ⇒ reviewed_by+review_note`) + `reviewed_by ≠ subject`. **D5 "otomatik ceza yok"
yapıyla garanti:** tablo tüm ledger'lardan **izole** — point_ledger / bonus_ledger / bonus_* /
compensation'a **FK/trigger/yazım yok**; `confirmed` geçişi **yan-etkisiz** (test: confirm sonrası
point_ledger & bonus_ledger satır sayısı **değişmez**). **INSERT server-only** (kural motoru); review
(confirm/dismiss) = `has_role('hr') OR manages_team(team_of(subject))` (**`flag.review` permission
eklenmedi** — test 0001 permissions=20 korunacak); WITH CHECK `reviewed_by = auth.uid()`. `related_task_id`
FK'sız; **`bonus_period_id` (FK'sız) Phase 7-A (`0023`)'te eklendi** (period-scoped idempotency; container
dilimde yoktu). RLS: subject-own + own-team Manager + HR + Auditor; **Finance/Support hariç**. DELETE yasak;
audit insert/update. **Hariç (container dilimi):** detection motoru → **Phase 7-A done (`ffdea06`)**;
self-approval hard-block, anomaly_baselines/Z-score, ceza/ledger wiring, dispute/notification.

**notifications invariant'ları (14 §424-429 / 15 §139-142):** `notifications` **recipient-only delivery
sink** — kullanıcı bildirim kutusu; **tek-yön `unread→read` yaşam döngüsü** (`read` terminal, `read→unread`
reddedilir), recipient **kendi** bildirimini read yapar ve `read_at` **server-stamp**'lenir; oluşturma sonrası
kimlik (org/recipient/type/payload/link/created_at) **immutable**. **INSERT server-only** (service_role — event
üretimi motor/uygulama işi = hariç); **client DELETE yok ve `prevent_delete` yok** — bildirimler kişisel-veri,
**retention/TTL V1'e ertelendi** (OQ-DD-3), silme legal-retention yüzeyi değil (service_role ileride prune
edebilsin). **Audit trigger yok** (§429 audit: hayır). **Yeni permission/rol yok** — RLS yalnız
`current_org()` + `auth.uid()`; permission-katalog 20 korunur. **type enum yok** (yalnız non-empty CHECK);
`payload` JSON object CHECK; read-consistency CHECK. Same-org composite FK `(organization_id, recipient_id) →
memberships`. RLS: **recipient-only SELECT/UPDATE** — **HR/Auditor/Manager/Finance/Support hariç** (audit izi
`audit_logs`'ta, burada değil). **Hariç:** email/push/realtime delivery motoru, notification preferences,
retention job, app/UI/API.

**exports invariant'ları (14 §444-451 / 15 §149-152 / 16 §8; SI-3/AD6/SI-15):** `exports` **payout export
kaydı/container** — üretim (generation) motoru YOK. **Finance INSERT** mevcut `payout.export` izniyle (yeni izin
yok — katalog 20); **actor integrity: `exported_by = auth.uid()`** (Finance başka kullanıcıyı exporter olarak
kaydedemez). **`snapshot_id` NOT NULL** (SI-3/INV-7 — snapshot'sız export yok). **AD6/SI-15 gate:** SECURITY
DEFINER trigger `snapshot.calculation_run_id → bonus_allocations` üzerinde `pending_missing_cap_basis` var mı
diye bakar (status **veya** cap_applied) — yalnız snapshot satırına bakmaz; varsa export **bloklanır**. **E:**
`exports.bonus_period_id` = snapshot'ın period'u. **Append-only client posture:** authenticated **UPDATE/DELETE
yok**; **`prevent_delete`** retention (finansal iz, silinmez). **Audit on INSERT** (`exports.insert`). RLS:
**Finance + Auditor SELECT** — **HR/Manager/Employee/Support hariç**. **Phase 6-c (`77615e3`)'te eklendi:**
`produce_payout_export()` (period `approved→exported` + exports record); `mark_payout_paid()` (period
`exported→closed` + per-employee `payout_marked_paid` ledger); BL-3 deferred payout-cap trigger;
`v_finance_payout` + `v_finance_period_totals` (security_invoker). **Hâlâ hariç:** CSV/XLSX/storage dosya yazımı, checksum,
notifications, clawback (D2 gated), app/API/UI.

**Phase 4 task/review core invariant'ları (04/14/15/16; D3/AD4/AD5):** `tasks` **status machine**
(`draft→assigned→in_progress→submitted→needs_revision↺→approved|rejected`; cancelled/archived terminal) — yasak
geçişler (skip-state, `approved→in_progress`, mid-state insert); **DELETE yasak** (cancel/archive = status).
`task_events` **auto-written append-only history** (`log_task_event` trigger — AD4 kaynağı; UPDATE/DELETE yasak,
client write path yok). `task_reviews` **append-only karar** — **review-driven transition**: `task_reviews`
INSERT, SECURITY DEFINER `apply_review_to_task` trigger'ı ile task status'unu uygular; **doğrudan client
approve/reject/needs_revision reddedilir** (yalnız güvenilir sunucu/definer bağlamı bu state'lere geçebilir).
**Self-approval hard block (AD4):** `reviewer_id <> assignee` — review INSERT'te (SECURITY DEFINER, tasks okur) +
tasks CHECK + transition belt. **D3:** `approve ⇒ quality <> poor` (CHECK). **AD4 timing:** submit `submitted_at`
damgalar; `needs_revision` `revision_count++` ve `submitted_at`'i korur; resubmit tazeler — pgTAP kanıtlı.
`complexity/impact/quality/timeliness` enum'ları **doc 04**'ten. Same-org composite FK'ler
(team/assignee/creator/reviewer/policy-version; events/reviews→tasks; actor→memberships). **RLS:** assignee/
creator/reviewer/`manages_team`/HR/Auditor + **support-grant top-level OR** (support üyeliksiz, `current_org()`
null → ayrı OR dalı); events/reviews **task görünürlüğünü miras alır**; **Finance ham task/review/event
göremez** (SI-12). **Yeni permission/rol yok** — task.create/assign/submit/review mevcut (katalog 20). Phase 4
DB slice'ında approve ledger yazmazdı; **Phase 5 (0020) artık approve'da tek `task_approved` satırı yazar** (0013
bu doğruyu test eder). **Hariç (Phase 4):** `task_assignments`/`task_comments`/`task_attachments` (+storage),
projects/objectives, period-lock guard, notifications, app/UI/API.

**Phase 5 scoring engine invariant'ları (04; D3/AD4/AD5/AD7/SI-1/SI-11/SI-12):** görev `approved` olunca
**deterministik server-side scoring**. `point_ledger`'a `task_id` (nullable) + `task_approved` event eklendi;
`tasks.final_points` **numeric**'e ALTER'landı. **SECURITY DEFINER BEFORE UPDATE** trigger (`score_task_on_approve`,
`trg_tasks_validate`'ten sonra çalışır) approved geçişinde: kilitli **published** policy version'dan çarpanları +
`revision_penalty_rule`'u okur, `final_points = base·complexity·impact·quality·timeliness·(1−min(rev·0.05,0.25))`
hesaplar (doc 04), `NEW.final_points`'i set eder ve **tek** `task_approved` kazanç satırını yazar (points_delta
= final_points; breakdown `point_ledger.metadata`'da). **SI-1 idempotency:** partial unique index — task başına
tek `task_approved` satırı. **Raw numeric, DB yuvarlama yok.** **AD4:** timeliness onaylayan
`task_reviews.timeliness`'ten; geç onay cezalandırmaz. **AD5:** `collaboration_score` yalnız metadata; final'i
etkilemez. **D3:** approve+quality=poor scoring'den önce reddedilir. **AD7:** draft/yayınlanmamış version score
edemez (trigger raise). Güvenilir **direct-approve (review'suz) scoring'i atlar**; review-driven approve'lar score
eder. **Server-only** (SI-11 — client scoring yetkisi yok). Seed'de mevcut published `d2`/`b-d2` policy version
multipliers + penalty rule doc-04 değerleriyle **dolduruldu** (published-immutability UPDATE'i engellediğinden INSERT
değerinde). **Finance ham `point_ledger`'dan hariç** (SI-12). **Yeni permission/rol yok** (katalog 20). **bonus_ledger/
bonus değişikliği yok; app/UI/API değişikliği yok.** **Hariç (5-b/ertelenen):** manual override/adjustment
(`point.override` 2-step → `manual_adjustment`); timeliness'in submitted_at/due_date'ten türetimi (reviewer/app);
breakdown UI.

**Phase 6 bonus engine invariant'ları (05 §8; D1/D2/D6/D10/AD6/AD7/AD8/AD9/AD10/SI-13):** `run_bonus_calculation(org,
period, pool, idempotency_key, triggered_by)` **SECURITY DEFINER, server-only** motor — mevcut `0013`
container'larını doldurur, **yeni tablo/permission yok** (katalog 20). **Locked period + locked pool** guard (AD10;
değilse `23514`). Approved points = `point_ledger task_approved` × `tasks.approved_at` **period içi**. **Safe Pro-Rata**
`W_individual=1.0`, malus yok (D1/D2); role/quality/team faktör=1; eligibility/proration yalnız **cap**'e (D10). Cap =
`compensation_records.cap_basis_minor × cap_rate × proration`; **cap basis yoksa `pending_missing_cap_basis`** —
**unlimited cap yok** (AD6). **T_org + AD8:** T≤1 kesinti undistributed'a; **T=1.2 top-up'suz pool'u aşamaz**, top-up'la
1.2× (`pool_ref`). **Largest-remainder kuruş** (yalnız uncapped satırlar, tie-break `employee_id` asc; cap residual
**yeniden dağıtılmaz** — D6). Yazar: `bonus_allocations` (run 'running' iken) + **tek immutable**
`bonus_allocation_snapshot` (faktörler + `undistributed_remainder` — AD7/SI-14); run **completed** (0013 freeze) +
period **locked→calculated**. **Idempotent** `(org, idempotency_key)` (varsa snapshot döner). `Σfinal + undistributed =
pool_ref` (SI-13). **`bonus_ledger` accrual YOK** — Phase 6-b'ye ertelendi (accrual/snapshot-approval boundary).
**Finance raw-excluded** (SI-12). **Verified:** `09` §8 worked example birebir (3.177.630 / 3.177.629 / 953.289 /
2.691.452; Σ=pool; undistributed 0; tie-break Ali<Ayşe) + cap/D6 residual + AD8 yes/no + T_org=0 + Σadj=0 +
single-eligible + AD6 pending. Tek in-slice **test-only** fix: `s.top_up_applied` (ambiguous kolon). **Hariç:**
accrual posting + snapshot-approval → **Phase 6-b done (`a65013d`)**; payout/export wiring, recalculation, UI/API sonraki fazlar.

**Phase 6-b bonus_ledger accrual invariant'ları (ADR-006/017; D2/AD6/AD8; BL-1/BL-2; SI-3/SI-7/SI-12/SI-13):**
`post_bonus_accrual(org, period, triggered_by)` **SECURITY DEFINER, server-only** — mevcut `bonus_ledger` (0014)
container'ına yazar, **yeni tablo/permission yok** (katalog 20). **Snapshot-approval boundary (ADR-006):** approval =
`bonus_periods` **`calculated→approved`** (mevcut geçiş; HR/`period.manage`; validator değişmedi; audit'lenir). Snapshot
**tam immutable kaldı** (approval period-level; snapshot `approved_by/approved_at` kolonları forward-compat/kullanılmaz —
OQ-3 onay audit_logs ile). Engine: approved period + **tek completed run** (OQ-5 fail-closed) → **AD6 gate** (allocation
`pending_missing_cap_basis` ise blok — provisional accrual yok) → **tek balanced transaction** (`debit pool = Σfinal` /
`credit accrual` per employee; `bonus_accrual`). **Idempotent** per snapshot (varsa no-op). **BL-1** append-only (0014);
**Σdebit=Σcredit** (0014 deferred balance); **BL-2** yeni **`DEFERRABLE INITIALLY DEFERRED` trigger** `Σaccrual ≤ pool_ref`
(AD8-aware: snapshot `pool_ref_minor` / top-up bütçesi). **BL-3** (payout ≤ accrual) **Phase 6-c (`0027`)'te eklendi**
(6-b'de producer yoktu — OQ-2). **OQ-1:** approval + posting **iki ayrı adım**. **Finance+Auditor raw read; server-only** (SI-12).
**Verified:** worked example accrual birebir (credits 3.177.630 / 3.177.629 / 953.289 / 2.691.452; debit pool 10.000.000;
Σdebit=Σcredit) + approval gate + idempotency + BL-2 red + AD6 blok + append-only + RLS. İki in-slice **test-only** fix
(Section A query'lerini Org C'ye scope + `::bigint` cast); **migration/engine bug yok**. **Hariç (sonraki):** payout/export
engine + `payout_exported`/`payout_marked_paid` wiring + `v_finance_*`, clawback, dispute→reversal orchestration, UI/API.

**Phase 6-d bonus engine authz hardening invariant'ları (AD1):** `run_bonus_calculation` (0021) ve
`post_bonus_accrual` (0022) giriş-authz'ı `has_permission('period.manage') OR current_user not in
('authenticated','anon')` idi; **SECURITY DEFINER içinde `current_user` = fonksiyon sahibi** (postgres, çağıran
değil) → ikinci clause her zaman TRUE → `period.manage` fiilen hiç zorlanmıyordu (latent authz zayıflığı). `0024`
her iki fonksiyonu **`CREATE OR REPLACE` ile, gövde/mantık/formül/ledger/imza birebir korunarak** yeniden
tanımlar; **yalnızca** authz clause'u `current_user not in (...)` → **`auth.uid() IS NULL`** olur (0023'teki doğru
"trusted server/job = authenticated JWT identity yok" sinyali; authenticated kullanıcı her zaman JWT `sub` taşır).
İmza değişmediği için **mevcut GRANT'lar + COMMENT'ler korunur** (aynı OID); **yeni tablo/permission yok** (katalog
20); seed değişmez; **0023'e dokunulmaz**. Migration **idempotent** (`CREATE OR REPLACE`; `db reset` temiz
yeniden uygular). **Davranış değişikliği tek yönlü sıkılaştırma:** authenticated + `period.manage`'siz çağıran
artık `42501` alır (AD1 — authz DB'den, ambient session state'ten değil). **Runtime tüketici yok** (app/client bu
server-only fonksiyonları çağırmıyor) → hiçbir çağıran kırılmaz. **Regresyon yok:** 0015/0016 engine çağrılarının
hepsi **trusted (`auth.uid()` null) bağlamda** (RLS rol-switch'inden önce) koştuğu için yeni authz'ı geçer.
**Verified (`tests/0018`):** (a) authenticated Finance c4 (period.manage yok) → `42501`; (b) authenticated HR c3
(period.manage var) → authz geçer → `23503` (bogus period); (c) trusted context (`auth.uid()` null) → authz geçer →
`23503` — her ikisi için (`run_bonus_calculation` + `post_bonus_accrual`; authz her lookup'tan önce raise ettiğinden
bogus uuid yeterli). **Hariç:** fonksiyon mantığı, dispute 7-B/7-C, payout/export, app/UI.

**Phase 7-A anti-gaming detection engine invariant'ları (08; D5/OQ-1..OQ-3):** `run_anti_gaming_scan(org,
bonus_period_id?)` **SECURITY DEFINER, server-only** orkestratör mevcut `anti_gaming_flags` (0016) container'ına
flag üretir — **yeni tablo/permission yok** (katalog 20). Dört `detect_*` fonksiyonu: **duplicate_task** (aynı
assignee + normalize `lower(btrim(title))` 24h içinde → sonraki task'e flag), **tiny_task_splitting** (aynı
assignee ≥3 task `base_points<5` 1h içinde), **same_reviewer_concentration** (period içi bir reviewer payı >0.80
ve toplam ≥3 approval), **period_end_spike** (son-3-gün `task_approved` point gain > 3× period günlük ortalaması).
Eşikler **hardcoded** (OQ-1; `organization_settings` kolon V1'e ertelendi). **OQ-2 dual idempotency:**
`anti_gaming_flags`'a **FK-less `bonus_period_id`** kolonu (mevcut FK-less `related_task_id` gibi — D5 "bonus_*
tablolara FK yok" korunur) + **iki partial unique index** (task-scoped `(org, rule, subject, related_task_id)` /
period-scoped `(org, rule, subject, bonus_period_id)`); re-scan **flag eklemez**. **OQ-3:** detection yalnız
**explicit `run_anti_gaming_scan()`** çağrısıyla (HR/job); approve anında otomatik değil. **D5 izolasyon
korundu:** detect fonksiyonları `tasks`/`task_reviews`/`point_ledger`/`bonus_periods` **okur**, **yalnız
`anti_gaming_flags`'a yazar** — point_ledger/bonus_ledger/bonus_*/compensation'a **yazım/FK/trigger yok**; bir scan
**finansal yan-etki üretmez** (test: scan sonrası point_ledger/bonus_ledger satır sayısı değişmez). **Authz:**
`has_role('hr') OR auth.uid() IS NULL` (güvenilir sunucu/job bağlamı) — non-HR authenticated `42501` alır;
`detect_*` grant'ları yalnız `service_role` (authenticated'tan revoke). **Verified:** her kural pozitif/negatif +
idempotency (re-scan 0 yeni flag) + D5 no-side-effect + server-only (non-HR reddi, `detect_*` çağrılamaz, direct
flag INSERT reddi). Bir **in-slice migration fix:** `run_anti_gaming_scan` authz'ı ilk yazımda `current_user not
in ('authenticated','anon')` idi — SECURITY DEFINER'da `current_user` = fonksiyon **sahibi** (postgres) olduğundan
her zaman "trusted" oluyordu; `auth.uid() IS NULL`-tabanlı doğru kontrole çekildi (test #11 kırmızı→yeşil).
**Hariç (gated):** point_ledger/bonus_ledger yazımı, dispute wiring (**7-B done `70ba400`**; **7-C done `8941089`**),
`organization_settings` eşik kolonu, `self_approval_attempt` trail, app/UI/API.

**Phase 7-B dispute point adjustment invariant'ları (07 §62; D9/D2/ADR-005):** `apply_dispute_point_adjustment(
p_dispute_id, p_points_delta, p_reason, p_actor)` **SECURITY DEFINER, server-only** — resolved+accepted dispute'un
puan etkisini mevcut `point_ledger` (0009) container'ına yazar; **yeni tablo/permission yok** (katalog 20). Sıralı
kontroller: **authz** (`has_permission('dispute.resolve') OR auth.uid() IS NULL` — dispute'u çözen reviewer veya
güvenilir server/job; yoksa `42501`) → **non-zero delta** (OQ-7B-5; 0 → `23514`) → **dispute yükle** (org+employee
dispute satırından **türetilir** — cross-org param yok, SI-7) → **precondition fail-closed** (OQ-7B-3: `status in
('resolved','closed') AND resolution='accepted'` değilse `23514`, satır yazılmaz) → **idempotency** (dispute başına
mevcut `dispute_adjustment` varsa no-op, id döner) → **insert** (employee = `complainant_id` — OQ-7B-2; tek delta).
**Şema (additive, 0020 precedent):** `point_ledger`'a `dispute_id uuid` + **same-org composite FK `(dispute_id,
organization_id) → disputes`** (SI-7); event_type CHECK **DROP+ADD (aynı isim)** → `dispute_adjustment` (0003
regresyon korunur); `point_ledger_dispute_adjustment_chk` (dispute_id NOT NULL + reverses_entry_id NULL); **partial
unique index** `(dispute_id) where event_type='dispute_adjustment'` (idempotency backstop → 23505). **Audit:** INSERT
audit WHEN clause **DROP+CREATE** ile `dispute_adjustment` eklendi (spec §62 "+ audit"); **`task_approved` yine
audit'siz** (0020 davranışı korundu). **Append-only** korundu (prevent_mutation; UPDATE/DELETE → `23001`). **D2
ihlali yok** (puan düzeltmesi = para/prim clawback değil; para tarafı **7-C done `8941089`** — balanced bonus_ledger reversal); **D9** resolve adımında (0015)
enforce, 7-B yalnız accepted sonrası uygular. **Verified (`tests/0019`):** pozitif (tek satır/delta/employee) +
rejected/under_review/zero-delta `23514` + idempotency (no-op + 23505) + dispute_id'siz `23514` + invalid event_type
`23514` (0003 regresyon) + audit satırı + append-only `23001` + cross-tenant FK `23503` + employee `42501`. OQ-7B-1..5
kilitli. **Hariç:** dispute_type routing, app/UI/API. dispute_adjustment → bonus bazı **Phase 7-D'de giderildi (`31c226f`)**. (**7-C done `8941089`**: balanced bonus_ledger reversal + run superseded + period `approved→calculated`; C-c1 reduced scope.)

**Phase 7-D dispute_adjustment → bonus basis invariant'ları (05; 07 §63; D1/D2/OQ-7D-1..7):**
`run_bonus_calculation()` **CREATE OR REPLACE** (aynı imza → grant/OID korundu; 6-d `auth.uid() IS NULL` authz korundu).
**Üç yapısal değişiklik:** (1) `point_ledger` + nullable `bonus_period_id` + same-org **composite FK → `bonus_periods`**
(SI-7) + `point_ledger_bonus_period_event_chk` CHECK — `dispute_adjustment ⇒ bonus_period_id NOT NULL`, diğer event'ler
⇒ NULL (0028 `ALTER`); (2) engine **period gate** `IN('locked','calculated')` (OQ-7D-3 — `calculated` period üzerinde
dispute-adjusted re-run çalışır; 0015 `23514` gate mesajı birebir korundu — regresyon yok); (3) `pts` CTE = **NET
`approved_points`**: `task_approved` (tasks.approved_at period içi) + `dispute_adjustment` (bonus_period_id =
p_bonus_period_id — NULL task_id artık JOIN'de kaybolmaz). Net ≤ 0 → `adjusted_score ≤ 0` → mevcut `>0` filtresiyle
**dışlanır, 0 prim** (OQ-7D-4; D2 malus değil). `bonus_allocations.factors` + **`dispute_adjustment_points`** alt-alanı
(OQ-7D-5; dispute yoksa 0, yine de mevcut — izlenebilirlik). `apply_dispute_point_adjustment()` **DROP+CREATE** (imza
değişti → 4-arg yerine 5-arg: +`p_bonus_period_id uuid DEFAULT NULL`; CREATE OR REPLACE olmaz, grant re-issue gerekli;
gövde 0025 birebir + INSERT'e `bonus_period_id` eklendi). **0019 test-only fix (3 satır):** yeni CHECK `dispute_adjustment
⇒ bonus_period_id NOT NULL` gerektirdiğinden #2 (positive apply) + #7 (23505 backstop direct insert) + #12 (cross-tenant)
satırlarına `p_bonus_period_id` eklendi — zorunlu minimum; migration/engine bug yok. Authz değişmedi; **katalog 20
korundu** (yeni permission yok). **Regresyon:** 0015 (engine gate mesajı birebir korundu) + 0016/0019/0021 yeşil.
**SI-13 korundu** (Σfinal + undistributed = pool_ref; 0022 Section A#7 kanıtlı). **Verified:** `db reset` 0001..0028 +
seed; `test db` → **Files=22, Tests=797, PASS, Failed=0**. D1/D2/OQ-7D-1..7 kilitli.

**✅ Güvenlik gözlemi ÇÖZÜLDÜ (Phase 6-d, commit `0b8b34a`):** `run_bonus_calculation` (0021) + `post_bonus_accrual`
(0022) SECURITY DEFINER'da etkisiz olan `current_user not in ('authenticated','anon')` authz'ı **`auth.uid() IS
NULL`**-tabanlı doğru kontrole çekildi (bkz. yukarıdaki Phase 6-d invariant'ları). Böylece 0021/0022/0023'ün üçü de
artık aynı doğru "trusted server/job = JWT identity yok" desenini kullanıyor; latent zayıflık kalmadı.

**App katmanı (Next.js/API/UI): ✅ foundation scaffold done** (commit `a8b05ac`, verified 2026-07-31).

**Phase 3.5 app foundation scaffold invariant'ları:** **Next.js 16.2.12 + React 19 + TypeScript strict + App
Router**. npm package stratejisi: mevcut **Supabase CLI devDependency korundu**, `package-lock` yeniden üretildi
(merge, replace değil). **Supabase clients:** browser anon (`client.ts`), server anon cookie client
(`server.ts` — RLS-enforced varsayılan yol), **guarded + unused service_role admin client** (`admin.ts`).
**service_role boundary (SI-11):** `import 'server-only'` ilk satır; **client/browser modülünden import yok**;
`SUPABASE_SERVICE_ROLE_KEY` yalnız throw-eden guard'da (env.ts) — **NEXT_PUBLIC ile sızıntı yok**. **Auth/session:**
`@supabase/ssr` + `auth.getUser()` (identity server-side doğrulanır). **Authorization DB/RLS kaynaklı** —
`role_permissions`; **JWT yalnız identity** (AD1). **Single active org** DB membership/cookie ile; multi-org
switcher ertelendi (Decision F). **Next 16:** `proxy.ts`, deprecated `middleware.ts`'in yerine (session refresh +
`/dashboard` guard). **`turbopack.root`/`outputFileTracingRoot`** proje köküne pinlendi (parent-lockfile
workspace-root uyarısı yok). **Tailwind (v3.4) + shadcn-uyumlu base UI primitives** (button/card/badge/skeleton).
**Zod Server Action validation wrapper** (`validatedAction`). **App shell rotaları:** login, auth callback,
guarded dashboard, unauthorized (403), health, error/not-found. **Vitest baseline** (env, service_role boundary,
validation wrapper, RBAC — 13 test) + **Playwright config** (E2E CI dışı). **CI:** `npm ci` + typecheck + lint +
unit; **E2E/pgTAP CI ertelendi** (Phase 9). **Sentry placeholder only** — env + `instrumentation.ts` no-op;
**`@sentry/nextjs` SDK ertelendi** (Next 16 uyumsuz, peer Next 15'e kadar). **Hariç:** Phase 4 domain logic,
scoring/bonus/export/notification engine'leri, production deploy. **Doğrulama:** `npm run typecheck → PASS`,
`npm run lint → PASS`, `npm run test → PASS (4 files, 13 tests)`, `npm run build → PASS` (workspace-root ve
deprecated-middleware uyarısı yok).

**✅ Doküman senkron güncel (2026-08-10):** `supabase/README.md`, `docs/planning/12`, `docs/planning/19`, bu dosya —
0001..0028 (3A/3B/comp/bonus-P/P/bonus-C/E/bonus-C/R/A/S/bonus-ledger/disputes/anti-gaming/notifications/exports,
**Phase 4 task/review core** `148667e`, **Phase 5 scoring engine** `aa47e40`, **Phase 6 bonus engine** `0c54fba`,
**Phase 6-b bonus_ledger accrual** `a65013d`, **Phase 6-c payout/export engine** `77615e3`,
**Phase 6-d bonus engine authz hardening** `0b8b34a`,
**Phase 7-A anti-gaming detection engine** `ffdea06`, **Phase 7-B dispute point adjustment** `70ba400`,
**Phase 7-C dispute bonus recalculation** `8941089`, **Phase 7-D dispute_adjustment → bonus basis** `31c226f`,
**Phase 7-E dispute bonus re-run orchestration** `3efe95d`) verified (**Files=23/Tests=814/PASS**), **Phase 3.5
app scaffold** (`a8b05ac`) durumunu yansıtıyor. **Phase 3 DB foundation + app scaffold + Phase 4 core + Phase 5
scoring + Phase 6 bonus calculation engine + Phase 6-b bonus_ledger accrual + Phase 6-c payout/export engine +
Phase 6-d authz hardening + Phase 7-A anti-gaming detection engine + Phase 7-B dispute point adjustment +
Phase 7-C dispute bonus recalculation + Phase 7-D dispute_adjustment → bonus basis + Phase 7-E dispute bonus
re-run orchestration tamamlandı; UI/API + Phase 5-b/8/9/10 gated.** `docs/planning/18`
Phase 3B DB planı olarak **korunur** (dokunulmadı); `docs/planning/19` Phase 7 planı (7-A + 7-B + 7-C + 7-D done;
migration numaraları 6-d sonrası **7-B→`0025` / 7-C→`0026` / 7-D→`0028`**). Ayrıca `docs/planning/14` idempotency + markdownlint
sync `dae4c6b`; `docs/adr/ADR-020` markdownlint `53d90de`; **docs hijyen (ADR-014 + Decision Lock) `17a964d`,
(ADR-006 + ADR-017) `98c0b59`, (doc-07 + doc-08) `91b6ce9`** ile tamamlandı.

---

## 2. Nereye gideceğiz (roadmap hatırlatma)

`docs/planning/12` fazları: **Phase 3 (DB/ledger foundation) → 4 (task/review) → 5 (scoring engine)
→ 6 (bonus engine) → 7 (anti-gaming+dispute) → 8 (dashboards/UX) → 9 (test/security) → 10 (prod)**.
Kural: güvenlik temeli (RLS/ledger) bitmeden feature fazı ilerlemez.

---

## 3. Ne yapmalıyız (todolist)

### A. Phase 3 DB foundation — tamamlanan dilimler  ✅

- [x] **3A — Foundation & RBAC** ✅ (verified 2026-06-24).
- [x] **3B-A — Scoring policy** ✅ (commit `dd9b861`, verified 2026-07-24).
- [x] **3B-B — Point ledger + `team_of` + append-only** ✅ (commit `f46ab49`, verified 2026-07-24).
- [x] **compensation_records + comp audit masking** ✅ (commit `c9cd0f2`, verified 2026-07-24; raw SELECT closed, justified read, masked audit).
- [x] **bonus_periods + bonus_pools** ✅ (commit `d04b954`, verified 2026-07-24; state machine, AD10 pool-lock, locked t_org+immutability, period identity immutability).
- [x] **bonus_pool_components + bonus_pool_eligibility** ✅ (commit `8f74e8d`, verified 2026-07-24; MVP individual=1.0 — D1; same-org employee via memberships composite FK; AD9 is_primary; server-only eligibility writes; inputs immutable once parent pool leaves draft — SI-4).
- [x] **bonus_calculation_runs + bonus_allocations + bonus_allocation_snapshots** ✅ (commit `e3bd1a3`, verified 2026-07-25; run machine + AD10 locked-period+locked-pool guard; idempotency `unique(org, key)`; completed-run allocation freeze; thin snapshot append-only; cap-not-exceeded + pending_missing_cap_basis; approved/exported/paid blocked; server-only).
- [x] **bonus_ledger (double-entry money)** ✅ (commit `71e68f7`, verified 2026-07-26; append-only; deferred `Σdebit=Σcredit` per (org, transaction_id) balance trigger; accrual ⇒ snapshot_id; idempotent accrual; only bonus_accrual+reversal writable; Finance/Auditor raw read only — HR/Employee/Manager/Support excluded; server-only; no posting engine / payout-export / clawback).
- [x] **disputes + dispute_events** ✅ (commit `1bf63fe`, verified 2026-07-26; mutable state machine + open sonrası identity immutability; append-only auto-history trigger; D9 stored decision_owner_id + owns_review_decision + reviewer≠owner/complainant CHECK; HR-only assign via has_role('hr') — no dispute.assign permission; due_at stored + sanity; Finance/Support excluded; no ledger/recalc/notification wiring).
- [x] **anti_gaming_flags** ✅ (commit `0c813e9`, verified 2026-07-31; mutable review lifecycle open→reviewing→confirmed/dismissed; D5 no-auto-punish — isolated from all ledgers, no FK/write to point_ledger/bonus_ledger/bonus_*/compensation, confirm inert & test-proven; review consistency + reviewer≠subject; server-only INSERT; review via has_role('hr') OR manages_team(team_of(subject)) — no flag.review permission; no bonus_period_id; related_task_id FK-less; Finance/Support excluded).
- [x] **notifications** ✅ (commit `fe1b81e`, verified 2026-07-31; recipient-only delivery sink; unread→read one-way lifecycle, recipient marks own read + read_at server-stamped; INSERT server-only; no client DELETE and no prevent_delete — retention/TTL V1; no audit trigger; no new permission/role; no type enum — type non-empty only; payload JSON object; same-org composite FK `(organization_id, recipient_id)→memberships`; RLS recipient-only SELECT/UPDATE — HR/Auditor/Manager/Finance/Support excluded).
- [x] **exports** ✅ (commit `b66350d`, verified 2026-07-31; payout export record/container — generation engine YOK; Finance INSERT via existing `payout.export`, actor integrity `exported_by = auth.uid()`; snapshot_id NOT NULL — SI-3; AD6/SI-15 gate via SECURITY DEFINER trigger on `snapshot.calculation_run_id → bonus_allocations` (pending_missing_cap_basis by status OR cap_applied); `exports.bonus_period_id` = snapshot period; append-only client posture — no authenticated UPDATE/DELETE; prevent_delete retention; audit on INSERT; RLS Finance + Auditor SELECT — HR/Manager/Employee/Support excluded; no new permission — catalog 20).
- [x] Docs/status sync (README + roadmap 12 + doc 18 + bu dosya). Ayrıca `docs/planning/14` idempotency+markdownlint sync (commit `dae4c6b`); `docs/adr/ADR-020` markdownlint (commit `53d90de`).

### B. Kalan Phase 3 DB-foundation dilimleri (SQL-only, app gerektirmez)  ✅ tamamlandı

> Her dilim = tablo(lar) + RLS ENABLE+FORCE + policy + bloklayıcı pgTAP + additive seed, **aynı dilimde**.

- [x] **Phase 3 DB foundation tamamlandı** — `exports` (commit `b66350d`) son dilimdi; 12 migration (`0001..0018`) + 12 bloklayıcı pgTAP suite (`0001..0012`, Tests=523) verified. Yeni tablo dilimi kalmadı; **app scaffold done**, sıradaki **Phase 4** (aşağıda C/D).
- [ ] (Ops., ileride) **projects**, **objectives** — minimal; yalnız ilgili feature fazı gerektirdiğinde, ayrı yetkiyle.

### C. App foundation (feature fazlarının ön koşulu)  ✅ tamamlandı

- [x] **Next.js + TypeScript scaffold** ✅ (commit `a8b05ac`, verified 2026-07-31; Next.js 16.2.12 + React 19 + TS strict + App Router; Supabase CLI devDep korundu + package-lock regenerated; browser anon / server anon cookie / guarded+unused service_role admin clients; `proxy.ts` (Next 16); `turbopack.root`/`outputFileTracingRoot` pinlendi; Tailwind + shadcn base UI; Zod `validatedAction`; Vitest 13 test + Playwright config; CI typecheck/lint/unit; Sentry placeholder — SDK Next 16 uyumsuzluğu nedeniyle ertelendi; typecheck/lint/test/build PASS).
- [x] **Auth akışı** (Supabase Auth `@supabase/ssr` + `auth.getUser()`) + org bağlamı (single active org via membership/cookie) + **RBAC okuması DB'den** (`role_permissions`, JWT identity-only — AD1) ✅. (DB `current_org()` ile RLS'e bağlama Phase 4 entegrasyon adımı.)

### D. Feature fazları (roadmap 4–10)  ⛔ her faz ayrı yetki

- [x] **Phase 4 — Task & Review Core (DB çekirdeği)** ✅ (commit `148667e`, verified 2026-08-01; `tasks` + `task_events` + `task_reviews`; status machine + DELETE yasak; auto-written append-only history; review-driven transition trigger — direct approve/reject/needs_revision blocked; self-approval hard block AD4; D3 approve⇒quality≠poor; AD4 timing; Finance excluded; support-grant top-level OR; no new permission — catalog 20; approve point_ledger üretmez — Phase 5 sınırı). **Ertelenen (feature-fazı):** `task_assignments`/`task_comments`/`task_attachments`, submit/review **Server Actions + UI** (ayrı yetki).
- [x] **Phase 5 — Scoring Engine** ✅ (commit `aa47e40`, verified 2026-08-01; approve→`point_ledger task_approved` + `tasks.final_points` numeric cache + breakdown metadata; `point_ledger` +`task_id`/`task_approved` + **SI-1 partial unique idempotency**; SECURITY DEFINER BEFORE UPDATE scoring trigger; doc-04 formula from published policy multipliers/`revision_penalty_rule`; raw numeric no rounding; timeliness from approving review — AD4; collaboration non-scoring — AD5; D3/AD7 guards; direct-approve-without-review skips; Finance excluded; no new permission — catalog 20; no bonus changes). **Ertelenen (5-b):** manual override/adjustment (`point.override` 2-step → `manual_adjustment`); breakdown UI.
- [x] **Phase 6 — Bonus Engine (Safe Pro-Rata calculation)** ✅ (commit `0c54fba`, verified 2026-08-09; `run_bonus_calculation()` locked period+pool → allocations + immutable snapshot; approved points from `point_ledger task_approved` × period; cap/AD6 pending; T_org/AD8 top-up; largest-remainder kuruş; Σ invariant SI-13; period locked→calculated; idempotent; `09` §8 worked example birebir; Files=15/Tests=665). **`bonus_ledger` accrual → Phase 6-b done (`a65013d`).**
- [x] **Phase 6-b — Bonus Ledger Accrual** ✅ (commit `a65013d`, verified 2026-08-09; `post_bonus_accrual()` SECURITY DEFINER server-only; approved period (`period.manage` `calculated→approved`) → tek balanced accrual (debit pool=Σfinal / credit accrual per employee); idempotent per snapshot; AD6 gate (pending → blok); **BL-2** deferred trigger Σaccrual≤pool_ref (AD8-aware); BL-3 → **Phase 6-c done**; snapshot immutable; catalog 20; Finance raw-excluded; Files=16/Tests=690). **Ertelenen:** clawback (D2 gated), dispute→reversal. **Payout/export engine → Phase 6-c done (`77615e3`).**
- [x] **Phase 6-c — Payout/Export Engine** ✅ (commit `77615e3`, verified 2026-08-10; `produce_payout_export()` SECURITY DEFINER server-only: approved period + AD6 gate → `exports` record + period `approved→exported`; `mark_payout_paid()` SECURITY DEFINER server-only: exported period → per-employee balanced `payout_marked_paid` (debit accrual / credit payout; doc-06 §2); **BL-3** DEFERRABLE INITIALLY DEFERRED trigger (payout ≤ net accrual, reversal-aware); idempotency BEFORE period gate; `payout_marked_paid` unlocked (`export_id NOT NULL` CHECK); `payout_exported`+`clawback_*` still blocked; `exports unique(id,org)` + `bonus_ledger export_id` same-org composite FK; `v_finance_payout`+`v_finance_period_totals` (security_invoker, NO raw points/quality/cap_basis/comp — SI-12); catalog 20; Files=21/Tests=780/PASS). **Ertelenen:** CSV/XLSX/storage dosya yazımı, clawback (D2 gated), app/API/UI.
- [x] **Phase 6-d — Bonus Engine Authz Hardening** ✅ (commit `0b8b34a`, verified 2026-08-09; `run_bonus_calculation` + `post_bonus_accrual` giriş-authz'ı `current_user not in (...)` → **`auth.uid() IS NULL`** — SECURITY DEFINER'da `current_user`=owner olduğundan eski kontrol etkisizdi; AD1 uyum; `CREATE OR REPLACE`, gövde/mantık/imza/grant değişmedi, aynı OID; catalog 20; no seed; regresyon yok (0015/0016 trusted bağlamda geçer); Files=18/Tests=720). 0021/0022/0023 artık aynı doğru deseni kullanıyor.
- [x] **Phase 7 — Anti-Gaming & Disputes** ✅ (7-A + 7-B + 7-C done; 7-D gated):
  - [x] **Phase 7-A — Anti-Gaming Detection Engine** ✅ (commit `ffdea06`, verified 2026-08-09; `run_anti_gaming_scan()` SECURITY DEFINER server-only orkestratör + 4 `detect_*` kuralı (duplicate_task/tiny_task_splitting/same_reviewer_concentration/period_end_spike); FK-less `bonus_period_id` + dual idempotency partial unique index (OQ-2); D5 izolasyon — yalnız flag yazar, ledger'a dokunmaz, scan finansal yan-etkisiz; authz `has_role('hr') OR auth.uid() IS NULL`; hardcoded eşikler OQ-1; no auto-punish/no auto-dispute — human-in-loop; katalog 20; Files=17/Tests=712).
  - [x] **Phase 7-B — Dispute Point Adjustment** ✅ (commit `70ba400`, verified 2026-08-09; `apply_dispute_point_adjustment()` SECURITY DEFINER server-only; resolved+accepted dispute → tek `point_ledger dispute_adjustment` delta (employee=complainant); fail-closed 23514; idempotent per dispute (partial unique index + 23505 backstop); `dispute_id` + same-org composite FK → disputes (SI-7); event_type CHECK DROP+ADD; audit WHEN clause genişletildi (task_approved hariç); authz `dispute.resolve OR auth.uid() IS NULL`; D2/D9 ihlali yok; append-only korundu; katalog 20; Files=19/Tests=737).
  - [x] **Phase 7-C — Dispute Bonus Recalculation** ✅ (commit `8941089`, verified 2026-08-10; `recalculate_bonus_after_dispute()` SECURITY DEFINER server-only; run **superseded** + period **`approved→calculated`** + mevcut bonus_ledger accrual **balanced reversal** (debit↔credit swap, yeni txn_id; BL-1 append-only; Σdebit=Σcredit); **paid-guard** 23514 (OQ-4/D2); idempotent (reversal varsa no-op); authz `has_permission('period.manage') OR auth.uid() IS NULL`; **C-c1 reduced scope** — yeni run/snapshot üretilmez; dispute_adjustment → bonus bazı **Phase 7-D'de giderildi**; katalog 20; Files=20/Tests=753).
  - [x] **Phase 7-D — Dispute_adjustment → Bonus Basis** ✅ (commit `31c226f`, verified 2026-08-10; `point_ledger` + nullable `bonus_period_id` + same-org composite FK + `point_ledger_bonus_period_event_chk` CHECK; `apply_dispute_point_adjustment()` DROP+CREATE +`p_bonus_period_id`; `run_bonus_calculation()` CREATE OR REPLACE — gate `IN('locked','calculated')` + NET `approved_points` (task_approved + dispute_adjustment by bonus_period_id) + `dispute_adjustment_points` factors kırılımı; net≤0 dışlanır; 0019 test-only fix (3 satır); 15 assertion; katalog 20; Files=22/Tests=797/PASS).
- [x] **Phase 8 — Dashboards & UX** ✅ (commit `7f51cfd`, verified tsc 2026-08-10; app UI layer — migration/DB değişikliği yok; 54 dosya; 8-A 5 Zod schema + 8-B 12 server action + 8-C 35 sayfa/feature component + 8-D nav activation + 8-E dashboard role summary; server-side `requirePermission`/RLS; Finance view'ları `createClient` ile; `npx tsc --noEmit` temiz). **Bilinen sınır:** leaderboard employee view = own-standing only (RLS); tam anonimleştirilmiş sıralama gelecekte bir ranking read-model gerektirir.
- [x] **Phase 9 — Testing & Security** ✅ DONE (commit `cae487a`, 2026-08-10): app-layer test suite — 9-A schemas (38 tests) + 9-B actions (43 tests) + 9-C security-boundary (4 invariants) + 9-D E2E route-guards (10 specs, env-gated); 98 Vitest tests / 11 files pass; E2E authored env-gated; no migration/DB change.
- [x] **Phase 10 — Production Readiness** ✅ DONE (commit `0403811`, 2026-08-10): Sentry instrumentation hook (Decision C, scaffolded — no-op until @sentry/nextjs supports Next 16); audit CSV export (AD3 comp masking); support access workflow UI (D4 time-bounded grants); nav item added; 12 files / 634 insertions; tsc clean + vitest 98/98 PASS; no migration/DB change. Known: Sentry no-op until @sentry/nextjs supports Next 16; audit raw-access log needs future DB function (DB-trigger-only).

---

## 4. Kalıcı hatırlatmalar / riskler

- Puan client'tan gelmez; point ledger append-only; düzeltme = reversal (ADR-005).
- Bonus snapshot olmadan payout/export yok; snapshot immutable (ADR-006).
- Malus/clawback = approval workflow, otomatik kesinti yok (D2).
- `compensation_records`: doğrudan raw SELECT yok; ham okuma reason + masked access audit ister; employee'ye kapalı (D7/AD3).
- Bonus: period pool locked olmadan lock edilemez (AD10); locked pool/period sessizce mutate edilemez (SI-4).
- Primary team tek kaynak: `team_memberships.is_primary` — `memberships.primary_team_id` **eklenmez** (AD9).
- KVKK / Türkiye iş hukuku = legal-review item (uzman onayı; kesin hüküm verilmez).

---

## 5. Mevcut durum ve sıradaki adım

**Phase 7-E dispute bonus re-run orchestration verified + committed + synced** (commit `3efe95d`; db reset
0001..0029 + seed, test db **Files=23/Tests=814/PASS/Failed=0**). `recalculate_bonus_after_dispute()` CREATE OR REPLACE:
7-C C-c1 reversal/supersede/approved→calculated + YENİ `run_bonus_calculation()` çağrısı (pool DB'den; idempotency key =
'disp-recalc-snap-'+reversed_snap). Döner: yeni snapshot id (7-D engine ile dispute_adjustment bazda). Period
`'calculated'` kalır — ADR-006 human re-approval. 0020 test-only fix (3 satır). Tüm önceki dilimler korunur: Phase 3 DB
foundation + Phase 3.5 app scaffold (`a8b05ac`) + Phase 4 (`148667e`) + Phase 5 (`aa47e40`) + Phase 6 (`0c54fba`) +
Phase 6-b (`a65013d`) + Phase 6-c (`77615e3`) + Phase 6-d (`0b8b34a`) + Phase 7-A (`ffdea06`) + Phase 7-B (`70ba400`) +
Phase 7-C (`8941089`) + Phase 7-D (`31c226f`) + Phase 7-E (`3efe95d`) — hepsi tamam.

**Phase 5-b manual point override verified + committed** (commit `b719053`; Phase 7-E'den sonra tamamlandı; db reset
0001..0030 + seed, test db **Files=24/Tests=831/PASS/Failed=0**). `apply_manual_point_adjustment()` — two-person point
override: caller + `p_actor` + `p_second_approver` üçü de `point.override` taşır (aynı org); distinct approvers; non-zero
delta; non-empty reason; employee + optional task same-org; tek `manual_adjustment` ledger satırı; mevcut audit trigger'ı
yazar; **şema değişikliği yok**; katalog 20. Deviation: employee-in-org kontrolü `team_memberships.profile_id` kullanır
(bu tabloda `employee_id` kolonu yok — 0004'ten doğrulandı).

**Phase 8 UI layer verified + committed** (commit `7f51cfd`; 54 dosya; **tsc temiz** — `npx tsc --noEmit`; DB
değişikliği yok — migration `0001..0030` + suite `0001..0024`/831 test aynı). App UI katmanı: 8-A 5 Zod schema
(`src/lib/validation/schemas/`) + 8-B 12 server action (`src/app/actions/`) + 8-C 35 sayfa/feature component +
8-D nav activation + 8-E dashboard role summary; authz server-side `requirePermission`/RLS, Finance view'ları
`createClient` ile. **Bilinen sınır:** leaderboard employee görünümü = yalnız own-standing (RLS); tam
anonimleştirilmiş cross-employee sıralama gelecekte bir SECURITY DEFINER ranking read-model (gated DB dilimi) gerektirir.

**Phase 9 tamamlandı** (commit `cae487a`, 2026-08-10): app-layer test suite — 9-A schemas + 9-B actions + 9-C boundary + 9-D E2E smoke; 98 Vitest tests / 11 files pass; E2E authored env-gated; no migration/DB change.

**Phase 10 tamamlandı** (commit `0403811`, 2026-08-10): production readiness — 12 dosya / 634 ekleme; Sentry DSN-gated instrumentation hook (Decision C, scaffolded); audit CSV export (AD3 comp masking); support access workflow UI (D4 time-bounded grants); nav item; tsc temiz + vitest 98/98 PASS; migration/DB değişikliği yok. Bilinen sınırlar: Sentry no-op — @sentry/nextjs Next 16'yı destekleyene kadar aktif değil (Decision C); audit raw-access log kaydı DB fonksiyonu gerektiriyor (gelecekte DB dilimi, şimdilik log üretemez).

**Tüm fazlar tamamlandı.** (Phase 3 DB foundation + Phase 3.5 scaffold + Phase 4 + Phase 5 + Phase 5-b + Phase 6 + Phase 6-b + Phase 6-c + Phase 6-d + Phase 7-A + Phase 7-B + Phase 7-C + Phase 7-D + Phase 7-E + Phase 8 + Phase 9 + Phase 10 — hepsi verified/done.)
