// Public API for the `audit` domain module (ENGINEERING-02A boundary).
// Consumers import only from `@/modules/audit` — never deep internal paths.
// Populated during the ENGINEERING-02F domain refactor.
export {};
