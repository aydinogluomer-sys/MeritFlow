# Deploy Evidence Log (ENGINEERING-12)

Record of every `deploy.yml` run for the ENGINEERING-12 DoD (the "gated prod migration" item —
one real run must pass through the `production` GitHub Environment gate, with its output recorded).

| Date | Run ID | Triggered by | apply | outcome | Reviewer | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| _pending_ | _pending_ | _pending_ | _pending_ | _pending_ | _pending_ | _pending_ |

After each dry-run, paste the **Evidence summary** printed by the workflow (in the job log and on
the run's step-summary page) into a new row above. The workflow prints a paste-ready table row;
fill in **Reviewer** (who approved the `production` environment gate) and any **Notes**.
